# Official model-size exactness record

This record answers a stricter question than “does the model run?”: does the
native runtime execute the same inference as the pinned Vortex/PyTorch model,
with zero unequal raw elements?

## Result

The official 1B, 7B, 20B, and 40B paths all completed real-checkpoint GPU
prefill and cached generation on A800 GPUs. The acceptance gate compared raw
dtype payloads, not a tolerance: every audited block output, generation logit,
and generated byte was exact. The 7B and 20B acceptance sets also include
selected internal tensors. For 40B, the internal trace found the first failing
FIR boundary; a focused reduction oracle then proved the correction for every
active history length from 1 through 127 before the full block/logit gate was
rerun.

| Model | Native topology | Audited execution | Result |
|---|---:|---|---|
| Evo 2 1B base | 1×A800 | 25 prefill blocks; 8-step cached decode | zero unequal elements |
| Evo 2 7B 1M | 1×A800 | 32 cached-decode blocks and internal layer-1 boundaries; 8×512 logits | zero unequal elements |
| Evo 2 20B 1M | 2×A800 | 24 prefill and cached-decode blocks; layer-0/1 operator boundaries; 8×512 logits | zero unequal elements |
| Evo 2 40B 1M | 4×A800 | 50 prefill and cached-decode blocks; layer-1 FIR/HCM first-divergence trace; 8×512 logits | zero unequal block/logit elements; zero FIR-oracle mismatches for history lengths 1–127 |

The oracle used Vortex commit
`8b00afebeac745d1f31e7e2788f0e0e39fa47637`, PyTorch
`2.13.0a0+8145d630e8.nv26.06`, and an explicit software model of the checkpoint's
fixed Transformer Engine E4M3/H100-QGMMA path where required. Native runs used
CUDA 12.8 on A800 (sm80). The reference and native executions used independent
code paths and, for the multi-GPU checks, different physical GPU pairs.

## Artifact identity

| Model | Official source SHA256 | Converted artifact/index SHA256 |
|---|---|---|
| 1B base | `8ffba7d0e6445a8f2c92d9ff1c4e772c7f73ca9179f5e0c699b8b0ca1b966f64` | `7bad301251de7bd57cba7a399a03a2c4ae8cf8404079e78907685d5b8fd243a0` |
| 7B 1M | `c66645929dc1b9c631f5be656da8726f38946315dc9167000a615dd626fcecf4` | `ee1757566fdf8616f706ebcce3ae65487beab6fab3246cff1762c799d7951e1e` |
| 20B 1M | `979b345dc700a9cae6b63f570d9b3f438e31a68dd8dc0bc0d8d2a0fcd3caf791` | `b8016e06cd84e93cc40b5a3ab3d7e647a9cbfd6d2720e9efd756af4c2f6a5ad9` |
| 40B 1M | `dd299612b1c1cdded0dfdcaf4d16f98fc97458261d80f4d662429f0ccb316bc3` | `1ab089a52a8efd06bf6f2d84cfd9f613c3910af6db9ba7c6312fffb801199e7c` |

Representative native output file hashes are:

- 1B cached logits: `9e80bc8dd98820526d6e916755467d6ae7e2cf2538d0ee9e6e69b51b2cff5442`
- 7B cached logits: `85e7c73e675dc3557e35bd5ccdf442bc524b919d3a9167739452e731ca6d1d24`
- 20B cached logits: `c93ee3704be992b3fb61e44a7cde365de46ced80dae73d0a9d926afe67f1bbd2`
- 40B cached logits: `21307db220a1700b784dcd732c5c3dc9979ed817afad882ea38cd1a320018848`

The matching reference files have the same hashes.

## Differences found and resolved

### 20B: the missing PyTorch load-time BF16 boundary

The first 20B attempt had the same top-1 token and cosine 0.999982, but it was
wrong: layer 0 was already different and 5,566 final-logit elements were
unequal. The first internal divergence was
`layer_0_mixer_input_projection`.

The 20B checkpoint uniquely stores Hyena projection weights as F32. Vortex's
`load_checkpoint()` first converts model parameters to BF16, copies the F32
checkpoint into them, and only then lets Transformer Engine quantize BF16 to
E4M3. The converter had performed F32-to-E4M3 directly, skipping a real
PyTorch rounding boundary. That changed 2,619,436 of the 201,326,592 layer-0
E4M3 codes.

`TorchE4M3Source.iter_chunks()` now performs F32 → BF16 → F32 → scaled E4M3,
exactly matching the Vortex load path. After reconversion, every 20B prefill
and cached-decode comparison passed raw-bit exact.

### 40B cached decode: `torch.sum` is not a serial loop

The first 40B cached run was exact through the prefill and early decode steps,
then diverged inside layer-1 HCM by two BF16 elements. Projection, short FIR,
pregate, and cache payloads were still exact; the first mismatch was the HCM
FIR output.

Vortex computes `torch.sum(fir_state * h, dim=-1)`. The native kernel had
accumulated 127 products serially. PyTorch reduces the contiguous row into 32
strided lanes and a warp-style `16, 8, 4, 2, 1` tree. Testing every active
history length from 1 through 127 showed that this tree reproduced the
PyTorch raw bits with zero mismatches.

`fir_decode_kernel` and continued FIR execution now use that reduction order,
preserve PyTorch's separate current-product and bias addition boundaries, and
track the dynamically sized Vortex FIR cache rather than treating leading
zero padding as semantic state. The corrected 40B run is exact for all 50
final block outputs, all cached logits, and generated bytes.

## Reproducing the gate

`tools/generate_vortex_vectors.py` creates the independent reference;
`tools/cuda_model_probe.cu` dumps native boundaries; and
`tools/compare_model_vectors.py --require-exact` fails on the first unequal raw
element while reporting its flat index and both payload hashes. See
[the GPU environment record](gpu02-environment.md) for container, driver, and
build details.
