--- a/embedding_pipeline/extractors/caduceus.py
+++ b/embedding_pipeline/extractors/caduceus.py
@@ -44,7 +44,6 @@ class CaduceusExtractor(BaseEmbeddingExtractor):
 
                 outputs = self.model(
                     input_ids=input_ids,
-                    attention_mask=attention_mask,
                 )
 
                 last_hidden = outputs.hidden_states[-1]  # (B, L, H)
