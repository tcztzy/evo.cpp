# SPEC

## §G GOAL

evo.cpp !成为本地、可嵌入、可移植的生物序列基础模型推理平台；Evo 2 exact inference 为首个且不可回退的参考实现，原生覆盖 Biohub ESMC 300M/600M/6B，并完整支持 GENEB v4 Table 4 全部 40 checkpoint 的 frozen embedding 推理与可复现评测。

## §C CONSTRAINTS

- C1: 注册表内 Evo 2 1B/7B/20B/40B exact profile !保持既有 raw-bit gate、确定性与显式失败语义；见 `docs/math-semantics.md`、`docs/model-size-validation.md`。
- C2: 生产 runtime !为 C++17；⊥ Python、PyTorch、libtorch、Vortex、Transformer Engine 运行时依赖。Python/PyTorch 仅限离线转换与 oracle。
- C3: Evo 2 exact artifact !继续使用严格 `evo2-runtime-v1` Safetensors；⊥为平台化重写已验证格式。其他 architecture/量化/容器只能使用显式独立 profile 或 adapter。
- C4: checkpoint 转换、mmap、manifest、FP8 extra-state 与原子发布契约 !保持 `docs/model-format.md`、`docs/checkpoint-conversion.md` 约束。
- C5: 训练/微调由 Savanna 或 NVIDIA BioNeMo 负责；本仓库只加载其推理产物与未来 adapter。
- C6: CUDA/Ampere 仍为 exact 主后端；新后端与 fast profile ⊥冒充 exact。
- C7: 平台覆盖 DNA 与蛋白质序列；多模型架构必须抽象扩展点，⊥在任一 architecture 私有类上继续堆叠公共接口。
- C8: 大 checkpoint、转换结果、数据集 ⊥提交 Git；下载缓存与 revision/hash !可审计。
- C9: ESMC canonical source !为 `biohub/ESMC-300M`、`biohub/ESMC-600M`、`biohub/ESMC-6B`；2024-12 HF repos 仅作 deprecated alias/source 说明，⊥隐式混载旧权重布局。
- C10: ESMC production runtime !为 C++17 CPU/CUDA/MPS；Python/PyTorch/官方 Transformers fork 仅限隔离的 converter/oracle，不进入产品依赖图。
- C11: ESMC v1 artifact !使用独立 `esmc-runtime-v1` strict Safetensors profile、F32 tensors、pinned source receipt；⊥伪装 `evo2-runtime-v1` 或 silent dtype/layout conversion。
- C12: ESMC 是最大 2048 tokens（含 `<cls>`/`<eos>`）的双向 masked encoder；v1 只承诺 logits/hidden-state embedding，⊥autoregressive generation、causal score、variant likelihood 或 recurrent chunking。
- C13: ESMC v1 CUDA !先支持单 device exact path；multi-GPU/CPU+GPU offload 必须 typed unsupported，⊥静默退化。
- C14: ESMC 接入 !保持 Evo 2/HyenaDNA artifact、tokenization、CLI、C ABI 与数值 gate 行为不变。
- C15: gpu02 Hugging Face 工作 !统一 `HF_HOME=/build/grp_icg/users/tang/.cache/huggingface`；repo cache !位于 `$HF_HOME/hub`，⊥在低配额 `$HOME` 重建 checkpoint cache。
- C16: MPS v1 !macOS arm64；C++17 core + 最小 Objective-C++ bridge；MPS/Metal 执行 GEMM，host 执行 state/nonlinear ops；⊥MLX/PyTorch runtime，⊥exact claim，⊥silent CPU fallback。
- C17: GENEB scope != 领域调研全集；! 精确覆盖 arXiv:2606.04525v4 Table 4 的 40 个 evaluated checkpoint；Table 5 的 13 个 excluded model 不得冒充已支持。
- C18: “supported” !表示 canonical checkpoint 可完成 pinned fetch/receipt、strict conversion/load、模型一致 tokenization/context/hidden-state/pooling 与真实权重 oracle gate；仅 registry/catalog 行 ≠ support。
- C19: GENEB production inference !遵守 C2；Python/PyTorch/JAX/官方自定义代码仅限隔离 converter/oracle/benchmark probe，⊥进入 `libevo` 依赖或推理调用链。
- C20: 权重/license 不同构；项目只分发 Apache-2.0 自有代码/元数据/转换器；第三方 checkpoint 按原 license/AUP 由用户获取，⊥重新打包非商业或授权不明权重。
- C21: GENEB 参考语义 !固定 `darlednik/GENEB@b465d2d6a11efbbc9a22c105e34832725ce50e05` + 已审计 patch/decision manifest；`geneb-v4-reference` 仅修复不可执行缺陷并保留 upstream 有效行为，`geneb-v4-normalized` 按记录独立消除 padding/batch 污染，二者结果不得混报。
- C22: 每个 GENEB checkpoint !至少有 portable C++ CPU F32/BF16 correctness path；CUDA/MPS capability 按真实 kernel/设备证据声明，缺少则 typed unsupported，⊥以隐式截断或 CPU fallback 冒充加速支持。
- C23: GENEB `runtime_support` 与 `benchmark_provenance` !独立；评测的“论文可复现”与“协议兼容” !分开声明；缺少可执行 reference extractor/官方 submission/digest 的模型可达 runtime supported 但只能产出 normalized protocol-compatible 结果，⊥声称重现论文榜单。

## §I INTERFACES

- I1 existing-cli: `evo -m MODEL -p DNA -n N --ctx N --gpu IDS` 与 `evo -m MODEL --score INPUT --ctx N --gpu IDS` !保持兼容。
- I2 cli: `evo run|score|embed|variant-score|serve|bench ...`; `-hf/--hf-repo REPO[@REV]` !解析已验证本地 HF cache artifact；⊥推理进程调用 Python downloader。
- I3 c-api: `include/evo/evo.h` 暴露 opaque `evo_model`、`evo_context`、`evo_batch`、`evo_sampler`; C ABI + version query。
- I4 artifact: strict Safetensors + index JSON + typed metadata；exact/fast/profile/revision/hash !可查询。
- I5 bio-input: FASTA、raw、stdin；后续 FASTQ/gzip/VCF/reference FASTA；record name、坐标、strand !保留。
- I6 output: scoring/bench JSONL；embedding NPY/Safetensors；generation stdout `raw|fasta`；错误→nonzero + typed status。
- I7 server: `/v1/generate`、`/v1/score`、`/v1/embeddings`、`/v1/variants`、`/health`、`/metrics`；请求可取消、限长、隔离 context。
- I8 install: `cmake --install` 提供 library、headers、CLI、CMake package；release 提供校验和与平台元数据，含 registry 对齐的 production architectures/artifact profiles。
- I9 esmc-source: `evo_fetch.py source esmc_{300m,600m,6b}` 获取 pinned config/tokenizer/checkpoint；`convert_esmc_checkpoint.py --receipt ...` 离线产出严格 runtime artifact。
- I10 esmc-cli: `evo logits -m MODEL --input INPUT --output DIR` 输出逐 token 64-way F32 NPY；既有 `evo embed ...` 输出选定 hidden layer，蛋白质输入默认添加 `<cls>`/`<eos>`。
- I11 esmc-artifact: metadata 至少含 architecture/profile/model-id/revision、layers/width/heads/vocab/context、tokenizer identity、tensor manifest 与 source receipt hash。
- I12 esmc-c-api: `evo_context_prefill` callback 返回全部 encoded positions logits；`evo_context_embed` layer `0` 为 token embedding、`1..n-1` 为前一 block 输出、`n` 为官方 post-final-LayerNorm embedding；`decode/generate/causal score` → typed unsupported。
- I13 mps-cli-c-api: `--backend mps [--profile mps-f32]`；`--gpu|--gpu-layers` → invalid_argument；C ABI append `EVO_BACKEND_MPS=3`,`EVO_STATUS_MPS=7`；ABI minor `1.4`。
- I14 geneb-catalog: `configs/geneb-models.json` → 精确 40 条；`geneb_model_id`(pinned model_meta key)、`paper_name`(Table 4)、`runtime_id` 三者双射；alias 另表且不计数；suite 固定 per-record raw safety cap；family/architecture/params/tokenizer/context/embedding/input_transform/source files、extractor commit+patch hash、oracle env/input digest、weight/code/tokenizer/dataset license+AUP+redistribution、`runtime_support`、`benchmark_provenance` 与 backend/promotion state !分项可查询；reference batching 固定 `{batch_size,order,split_boundary,final_batch,pad_to_batch_max,padding_side}`。
- I15 geneb-cli: `evo models --suite geneb [--json]`；`evo embed -m MODEL --input INPUT --output DIR --preset geneb-v4-{reference,normalized}` → 按 pinned preset 有界缓冲、保持输入顺序输出 per-record NPY + metadata，禁止同时传 `--layer|--pooling`；`geneb` alias 固定指向 normalized 且在 metadata 展开。
- I16 geneb-fetch: `evo_fetch.py source MODEL --catalog configs/geneb-models.json` → pinned HF/HTTP/Dataverse/Drive receipt；无法自动获取的授权 checkpoint → typed manual-source 指引。
- I17 geneb-artifact: strict family-specific Safetensors profile + typed tokenizer assets/forward config/embedding preset/source receipt；`input_transform` !声明 raw outer-trim/case/U→T/invalid/frame trim/crop side+offset/fixed pad/special-token/token-truncation policy与 tokenizer post-processor boundary 保留语义；reference batch !按 I14 以输入顺序每 8 条分组、train/test 边界处 flush、末批保持实际大小、仅 pad 到当批最长（方向按模型 preset）；不兼容族 !独立 architecture/ABI，⊥伪装 `evo2-runtime-v1`。
- I18 geneb-oracle: `tools/validate_geneb_models.py` → 40-model catalog/converter/load/token/hidden/pool gate summary JSON，每条证据含 `extractor_commit`、`normalization_patch_sha256`、`oracle_env`、`oracle_input_digest`；`tools/run_geneb.py` 使用 dataset revision `4edd705be573e48c585c2cf79dc320f9f43c7b04` 与 exact Python/NumPy/scikit-learn lock、完整 LogisticRegression kwargs/solver、thread env、submission schema 执行 100-task×`full|10shot|1shot` frozen probe，reference/normalized 输出分 namespace。
- I19 geneb-embed-abi: C ABI minor `1.5` append `evo_context_embed_ex`、size/versioned options `{preset,layer,pooling}` 与 callback-scoped `evo_embedding_result_info` metadata（resolved preset/hidden tap/pooling/original+effective length/crop+pad/token count/rows/columns），旧 `evo_context_embed` 保持；server `/v1/embeddings` 接受 `preset:"geneb-v4-reference|geneb-v4-normalized"` 且与 `layer|pooling` 互斥。

## §R RESEARCH

id|topic|finding|src
R1|llama.cpp platform|核心覆盖 C/C++ library、HF 直载、1.5–8 bit 量化、CPU/CUDA/Metal/HIP/Vulkan/SYCL 与 CPU+GPU hybrid|https://github.com/ggml-org/llama.cpp
R2|llama.cpp serving|server 提供 parallel decoding、continuous batching、embeddings、monitoring 与多用户支持|https://github.com/ggml-org/llama.cpp/blob/master/tools/server/README.md
R3|Evo 2 inference|官方推理面包含 forward scoring、intermediate embeddings、generation；最长 checkpoint context 1M bp|https://github.com/ArcInstitute/evo2
R4|Evo 2 training|官方把训练/微调交给 Savanna 或 NVIDIA BioNeMo|https://github.com/ArcInstitute/evo2#training-and-finetuning
R5|OpenGenome2 IO|raw 数据为大规模异构 FASTA；流式 record 处理优先于全量 materialization|https://huggingface.co/datasets/arcinstitute/opengenome2
R6|ESMC canonical models|Biohub 当前推荐 `ESMC-300M`/`ESMC-600M`/`ESMC-6B`；旧 `*-2024-12` repos 仅保留 backward compatibility|https://huggingface.co/biohub/ESMC-6B
R7|ESMC topology|三者分别为 `(d_model,heads,layers)=(960,15,30),(1152,18,36),(2560,40,80)`，vocab=64、max positions=2048、F32 source weights|https://huggingface.co/biohub/ESMC-300M/blob/a59b831785f907e96e6a246b1d142bfb76df31ee/config.json
R8|ESMC forward|官方实现为 pre-LN Transformer：scaled residual、Q/K LayerNorm、non-interleaved RoPE、bidirectional SDPA、SwiGLU、final LayerNorm 与 GELU/LayerNorm LM head|https://github.com/Biohub/transformers/blob/3a8956fb4d4ea16b0ec8e71deef2c2909b6a5cbf/src/transformers/models/esmc/modeling_esmc.py
R9|ESMC tokenizer|固定 64-slot vocab；33 个已分配 token，`<cls>=0,<pad>=1,<eos>=2,<unk>=3,<mask>=32`，character BPE 自动包围 cls/eos|https://github.com/Biohub/transformers/blob/3a8956fb4d4ea16b0ec8e71deef2c2909b6a5cbf/src/transformers/models/esmc/tokenization_esmc.py
R10|ESMC outputs|官方 masked-LM 返回 per-token logits、last hidden state 与可选全部 hidden states；⊥causal generation contract|https://huggingface.co/biohub/ESMC-6B
R11|ESMC license|Biohub ESM code/models 使用 MIT license，并要求遵循 Biohub Acceptable Use Policy|https://github.com/Biohub/esm/blob/26b0bc2b771e3e419ea74f445a5f35cc094a1509b6a5cbf/README.md#license
R12|ESMC weights|当前 300M/600M revision 分别为 `a59b831…`/`a7e8201…` 单 F32 Safetensors；6B `45b0fa5…` 为 6-shard F32 Safetensors + index|https://huggingface.co/biohub/ESMC-6B/tree/45b0fa5d7fb06faefbd5e3b89bdcef35d564e79a
R13|MPS GEMM|`MPSMatrixMultiplication` 执行 `C=alpha*op(A)*op(B)+beta*C`；matrix transpose + command-buffer encode 为公开 contract|https://developer.apple.com/documentation/metalperformanceshaders/mpsmatrixmultiplication
R14|MPS matrix storage|`MPSMatrix` row-major，底层使用 `MTLBuffer`；CPU/GPU coherency 遵守 Metal buffer 规则|https://developer.apple.com/documentation/metalperformanceshaders/mpsmatrix
R15|Metal CLI device|`MTLCreateSystemDefaultDevice()` 返回系统默认 GPU；macOS 非图形 CLI !显式链接 CoreGraphics|https://developer.apple.com/documentation/metal/mtlcreatesystemdefaultdevice%28%29?language=objc
R16|GitHub macOS GPU|标准 arm64 macOS runner ⊥GPU acceleration contract；8-core GPU hardware acceleration 仅由 macOS xlarge runner 明示|https://docs.github.com/en/actions/reference/runners/larger-runners#available-macos-larger-runners-and-labels
R17|GENEB model scope|v4 Table 4 定义 40 evaluated models：13 decoder、15 encoder + DeepGene、3 Hyena/StripedHyena、4 Mamba/Caduceus、2 JanusDNA、2 CNN-Transformer|https://arxiv.org/abs/2606.04525v4
R18|GENEB protocol|GENEB 对 frozen sequence representations 使用统一 probing protocol；官方 main 说明 40-model reference extractors 位于 dev branch|https://github.com/darlednik/GENEB/tree/b465d2d6a11efbbc9a22c105e34832725ce50e05/embedding_pipeline/extractors
R19|GENEB dataset|100 tasks/13 categories；task dataset immutable revision `4edd705be573e48c585c2cf79dc320f9f43c7b04`；probe=logistic regression，seeds=`13,17,42,123,997`|https://github.com/darlednik/GENEB/blob/b54d018903e7f6b874ee45b74e275936deff4cd3/benchmark/benchmark_spec.json
R20|GENEB exclusions|13 surveyed models 因 private/broken/missing code/计算或 wrapper 原因排除；Evo2 不在 evaluated 40 中|https://arxiv.org/abs/2606.04525v4
R21|GENEB source heterogeneity|official extractors 混用 HF AutoModel、自定义 PyTorch/JAX、Google Drive、Harvard Dataverse 与本地 checkpoint；转换层 !分离 source acquisition 与 runtime artifact|https://github.com/darlednik/GENEB/tree/b465d2d6a11efbbc9a22c105e34832725ce50e05/embedding_pipeline/extractors
R22|GENEB harness reproducibility|official requirements 仅给 NumPy/scikit-learn 开放下界，harness 继承 LogisticRegression 多个版本默认值，validator 不重算 metrics；可复现 runner !额外锁定环境/参数/输出|https://github.com/darlednik/GENEB/blob/b54d018903e7f6b874ee45b74e275936deff4cd3/harness/run_GENEB.py
R23|HyenaDNA long topology|pinned Medium-160k/Large-1M 均为 F32 `d_model=256,n_layer=8,d_inner=1024,hyena_order=2,filter_order=64,short_filter_order=3`；context=`160002|1000002`；官方 long conv=`rfft(F32,k,n=2L)/2L * rfft(F32,u,n=2L)`→`irfft(norm=forward)[:L]+u*D`|https://huggingface.co/LongSafari/hyenadna-large-1m-seqlen-hf/blob/0a629abf9c7f85b4ec9aa6a1aefa3adcf1907446/modeling_hyena.py
R24|Evo-1 topology|`evo-1-131k-base@1.1_fix=c206aab…` 为 width4096/layers32/heads32/inner10928/context131072；attention layers=`8,16,24`，其余 29 层 modal Hyena；source tensors=BF16 projections/norm/MLP + F32 complex poles/residues，parallel prefill 以 2L FFT 执行 modal convolution|https://huggingface.co/togethercomputer/evo-1-131k-base/blob/c206aab77ae5967a069c4200ecb1858588528c9d/model.py
R25|GENEB long Hyena semantics|HyenaDNA wrapper CPU=F32/CUDA=BF16、无 special tokens、final post-LN hidden 按 attention mask mean；Evo wrapper使用官方 byte tokenizer、`outputs[-1]` 全长 mean，但每 batch 只取第1条，∴reference 保留/normalized 修复必须分 namespace|https://github.com/darlednik/GENEB/tree/b465d2d6a11efbbc9a22c105e34832725ce50e05/embedding_pipeline/extractors
R26|GENEB Caduceus topology|Plant/PS/PH pinned config 均为 F32 Mamba1、expand=2/state=16/conv=4、双向 add 且仅 in/out projection tied；Plant/PS `rcps=true` 输出 `2*d_model`，PH `rcps=false`；PS/PH context=`131072|1024`，Plant 未声明有限上限|https://huggingface.co/kuleshov-group/caduceus-ps_seqlen-131k_d_model-256_n_layer-16/blob/d89eeb853136ea64da7feb3d0c8e909771b17ae6/config.json
R27|Caduceus RC/scan semantics|pinned remote code以 causal depthwise conv + selective scan 实现 Mamba1；每层 forward/reverse scan 后 add；RCPS embedding/wrapper 同时 flip sequence+channel，fused RMS residual order 与 final hidden tap 固定|https://huggingface.co/kuleshov-group/caduceus-ph_seqlen-1k_d_model-256_n_layer-4_lr-8e-3/blob/9865108985311772704c2ece0bac082153a6167b/modeling_caduceus.py
R28|eccDNAMamba semantics|pinned checkpoint=F32 `d_model=768,n_layer=24`；实际 remote wrapper 构造两套 Mamba2+GatedMLP backbone，Mamba2=`d_inner1536,state128,heads24,head_dim64,groups1,conv4`，双向 final hidden concat 后 `[768,1536]` projection；BPE 自动 prefix `[CLS]`、未声明有限 context|https://huggingface.co/eccDNAMamba/eccDNAMamba-1M/tree/36d95a3f783fa93640ce9e070ebde3f0ebed175d
R29|GENEB Caduceus extractor defect|pinned extractor给 `AutoModelForMaskedLM` 传 `attention_mask`，但三条 pinned `CaduceusForMaskedLM.forward` 均不接收该 kwarg，clean reference 在 forward 前 `TypeError`；未 hash patch 时只可 normalized/protocol-compatible|https://github.com/darlednik/GENEB/blob/b465d2d6a11efbbc9a22c105e34832725ce50e05/embedding_pipeline/extractors/caduceus.py
R30|JanusDNA canonical receipts|Harvard Dataverse V2 两检查点/配置当前 exact `(size,SHA256)`：with=`(108242317,94790a3f…bc3e)/(1608,ba89cced…346f)`，without=`(108361237,015cd69e…3df)/(1610,0fe6f1e8…df9f)`；source mutable，converter !仍要求逐文件 post-download receipt，⊥把观测 hash 冒充 revision|https://dataverse.harvard.edu/dataset.xhtml?persistentId=doi%3A10.7910%2FDVN%2FHDT0RN
R31|JanusDNA topology|两者均为 F32 width72/layers8/Mamba1 `inner144,state16,conv4,dt_rank5`、separate forward/reverse、仅 in/out projection storage-tied；odd layers=16-expert top2 SwiGLU MoE；with 另以 layer4 双向 causal attention替换 Mamba，最后均执行 custom FlexAttention+fusion+RMSNorm；config vocab12 在构造时 pad 至16-row embedding|https://github.com/Qihao-Duan/JanusDNA/blob/3f449542529a2948d73062514cf9844705a4277a/janusdna/modeling_janusdna.py
R32|GENEB JanusDNA semantics|pinned wrapper以 Caduceus SN tokenizer uppercase、无 special、right truncate/pad 至1024，mask仅用于最终 mean；加载时丢 torchmetrics/前缀、`strict=False` 忽略 LM head/缺失 final MLP，再把 `final_mlp=Identity`，但 forward 保留 residual add，∴last hidden=`2×post-final-RMSNorm`；with/without clean source manifest分别636/652个F32 entries|https://github.com/darlednik/GENEB/blob/b465d2d6a11efbbc9a22c105e34832725ce50e05/embedding_pipeline/extractors/janusdna.py
R33|PlantCaduceus context|官方训练仓/论文仅声明 512-bp pretraining/evaluation window，并明确 SSM 可处理更长 context、512 为计算折中；pinned HF config 无位置/长度 gate，tokenizer `model_max_length` 为 Transformers unknown sentinel，GENEB `truncation=True` 不会截到512，∴ advertised training length=512 与 executable hard max=unknown !分开|https://pmc.ncbi.nlm.nih.gov/articles/PMC12184517/
R34|JanusDNA Mamba inner norm|pinned Janus Mamba1 在 `x_proj` 切分后分别以 RMSNorm(eps=`1e-6`)归一化 dt(rank5)、B(state16)、C(state16)，再执行 dt projection/selective scan；Caduceus 未启用这三路 norm，∴共享 primitive !显式可选且 scale tensor manifest !由 adapter 决定|https://github.com/Qihao-Duan/JanusDNA/blob/3f449542529a2948d73062514cf9844705a4277a/janusdna/modeling_janusdna.py
R35|JanusDNA reference device/license|pinned GENEB wrapper 默认 `device=cpu`，而 Dataverse config 未覆盖 `use_mamba_kernels`、上游 constructor 默认 true，CPU forward 因要求 CUDA fast kernels 直接失败；clean official reference ∴blocked，native normalized CPU path !以独立 oracle验证。pinned source repo LICENSE 为 Apache-2.0，权重仍遵循 Dataverse CC0 receipt|https://github.com/Qihao-Duan/JanusDNA/blob/3f449542529a2948d73062514cf9844705a4277a/LICENSE
R36|eccDNAMamba Mamba2 dependency|official `mamba-ssm` `v2.2.4` tag 解析为 `95d8aba8a8c75aedcaa6143713b11e745e7cd0d9`；PyPI 2.2.4 sdist SHA256=`e4114c69302796c91b71e90032c2d974f611608fab331582a80de6eaf075efb9`，其 `mamba2.py`/`mlp.py` 与该 tag bit-identical，固定 z/x/B/C/dt、SSD、RMSNormGated 与 GatedMLP 语义|https://github.com/state-spaces/mamba/tree/95d8aba8a8c75aedcaa6143713b11e745e7cd0d9
R37|Enformer pinned artifact/topology|HF `EleutherAI/enformer-official-rough@affe5713…16c` 的 `pytorch_model.bin` exact `(1005149571,99b09d60…439c)`；570 tensor 中 283 组 `_trunk.*` 是 primary trunk exact alias、28个 I64 counter 与 output heads validate/drop，canonical trunk=269 F32/919500800 bytes。输入196608 one-hot bp→stride128 length1536→11 relative-attention blocks→center crop896→final pointwise width3072→spatial mean；GENEB undefined `seq_length` patch固定196608且 `use_tf_gamma=false`|https://huggingface.co/EleutherAI/enformer-official-rough/tree/affe5713ae9017460706a44108289b13c5fee16c
R38|SPACE pinned artifact/topology|HF `yangyz1230/space@63c9f501…2c18` 的 `pytorch_model.bin` exact `(2355245554,f22ce262…cc81)`；363 tensor=349 F32+14 I64，无 alias，validate/drop heads/tracks 后 canonical trunk=315 F32/2166370656 bytes。输入131072 one-hot bp→stride128 length1024→追加 human species row→11 blocks且每个 FFN 为4-expert top3 SpeciesMoE→移除 species row→center crop896→final width3072→spatial mean|https://huggingface.co/yangyz1230/space/tree/63c9f5011877cc20b3e9d2d63dcdb1d547e62c18
R39|DeepGene pinned RoFormer-only artifact|Google Drive weight exact `(355268434,4717e43c…99d6)`、config exact `(718,09a9d102…26f2)`；safe load得203个F32 tensor，canonical `roformer.*`=196，7个 MLM head validate/drop；仅 word embedding↔decoder weight、prediction bias↔decoder bias 两组 exact alias。GENEB只调用 V4096/H768/L12/A12/I3072/max5120 的 RoFormer：word+type0+LN、adjacent-pair Q/K RoPE、bidir attention、post-LN、erf-GELU，无 graph stage/final LN；手工输入顺序 CLS,SEP,payload，masked mean含两 special|https://github.com/wds-seu/DeepGene/tree/486343e5212361d6cd7ed03c624f430ed3d5f02e

## §V INVARIANTS

- V1: ∀ registered exact artifact → checkpoint signature、dtype、shape、数值 gate 与既有 generated bytes 不变。
- V2: ∀ production binary dependency/call chain → Python/PyTorch/libtorch/Vortex/Transformer Engine ∉ graph。
- V3: ∀ multi-record sequence input → reader 每次最多 materialize 1 record；⊥读取整个 FASTA 后再推理。
- V4: 默认 scoring host logits memory ∈ O(chunk_tokens×vocab)；仅显式 `--dump-logits` 可 materialize O(sequence_tokens×vocab)。
- V5: ∀ FASTA record → name、record order、sequence bytes 与错误行号保持；CRLF/wrapped lines 与既有严格 whitespace 语义不回退。
- V6: ∀ record → length 在模型 load/执行前或 record 进入执行时校验 `2 ≤ length ≤ ctx`; 错误包含 record name。
- V7: exact/fast/quantized/backend mode !显式输出；fast/quantized !拥有独立数值 + 生物任务 acceptance gate，⊥标记 exact。
- V8: model weights 与 mutable context/session state !分离；多个 context 可安全共享只读 model。
- V9: 同 build+artifact+mode+seed 的 greedy output !byte-identical。
- V10: unsupported model/profile/shape/dtype/backend/OOM !typed fail；⊥silent fallback。
- V11: embedding !支持指定 intermediate layer 与明确 pooling；shape、layer、dtype 写入输出 metadata。
- V12: variant scoring !记录 reference、alternate、window coordinates、strand 与归一化策略；reference mismatch → fail。
- V13: server ∀ request → 独立 context、输入上限、取消路径；dynamic batching ⊥混淆 record/session state。
- V14: public C ABI 仅 opaque handles；ABI version query + ownership/free pair !有 contract tests。
- V15: current I1 CLI scripts/tests !继续通过；新 command hierarchy 只能兼容扩展。
- V16: ∀ completed §T → named test 覆盖对应 invariant；full CPU suite !green，相关 CUDA/exact gate 在目标环境 !green。
- V17: sequence byte limit !在读取/append 前检查；超限输入 ⊥先 materialize 再拒绝。
- V18: streaming input 后续 record 失败 → 已输出 JSONL 仅含完整 record 行 + process nonzero；⊥半行或成功退出。
- V19: remote GPU build 缺依赖 → actionable path error；成功 build !写 source fingerprint；remote test !拒绝缺失/不匹配 fingerprint，⊥运行 stale binary。
- V20: public sequence IO ∀ multi-record path → only streaming callback API；⊥暴露全量 `vector<SequenceRecord>` 聚合 helper。
- V21: bench ∀ report → architecture、artifact/profile、backend、input identity、warmup、repetitions、token count、timing statistic !显式；失败/unsupported → typed nonzero。
- V22: ∀ production exact model ID → pinned real-checkpoint raw-bit gate evidence !存在；缺证据 ID !显式 experimental/unsupported，⊥继承同 family 证据。
- V23: ∀ maintained runner 使用 exact-unsupported model ID → !显式 non-exact profile 或断言 typed unsupported；⊥依赖默认 `exact`。
- V24: ESMC registry ∀ size → canonical HF ID/revision、Biohub hosted alias、topology 与 exact capability !显式；未知/legacy-incompatible source → typed fail。
- V25: ∀ ESMC protein sequence → tokenizer token IDs 与 R9 bit-exact；默认首尾分别为 0/2，`<mask>`/`|` 与已分配字符按官方 special-token/BPE 规则解析，非法/非词表 byte → 3，`encoded_length ≤ 2048` 在分配/执行前检查。
- V26: ESMC forward ∀ backend → R8 operation order、epsilon、RoPE positions、residual scale、exact GELU 与 F32 source semantics !固定；同输入 logits/final embedding 对官方 oracle 满足 `max_abs≤5e-3,mean_abs≤5e-4,cosine≥0.99999`，否则 fail。
- V27: ESMC capability !仅 `logits|embed`；generation/decode/causal score/variant → typed unsupported，⊥借用 Evo 2 sampling/scoring path。
- V28: ESMC conversion ∀ canonical size → pinned receipt/config/tokenizer、完整 tensor names/shapes/dtypes/sizes/hashes !先验证；缺失/多余 real tensor、非 F32、zero-byte extra-state 之外异常或 hash mismatch → 原子失败。
- V29: ESMC ∀ forward/embed → 单次完整 encoded sequence 执行双向 attention；⊥跨 chunk 复用 prefix state；embedding metadata !记录 point。
- V30: ESMC tests !含 tiny deterministic CPU/CUDA fixture、tokenizer vectors、converter corruption gates；gpu02 ∀三 canonical artifacts → conversion/load + short-sequence official logits/last-hidden oracle gate。
- V31: ESMC change 后 full CPU suite 与既有相关 gpu02 Evo 2/HyenaDNA gates !green；⊥修改已有模型生成/token/logit semantics。
- V32: ESMC CUDA v1 ∀ device selection → exactly one CUDA device；0/2+ devices 或 unsupported compute/runtime → typed fail，且失败前⊥部分输出。
- V33: ESMC hidden index !bit-exact 对齐 pinned Transformers `layers_to_collect`：`0=token_embedding`、`i∈[1,n-1]=block(i-1)_output`、`n=final_layer_norm`；⊥沿用旧 ESM SDK 的逐 block-output convention。
- V34: gpu02 ESMC fetch/conversion/oracle gate → !export C15 `HF_HOME` 且默认 `cache_dir=$HF_HOME/hub`；显式 override !可审计。
- V35: ∀ platform-level docs/help/release metadata → !表述 biological-sequence platform 且与 registry production architectures/artifact profiles 对齐；architecture-specific 页面 !显式限定 scope；⊥把 evo.cpp 全局等同 Evo 2 runtime。
- V36: MPS !显式选择、显式输出 backend/profile/kernel；device/framework/command-buffer/OOM failure → typed nonzero，⊥partial output/fallback。
- V37: MPS `mps-f32` ∀ registered architecture → operation order/token/cache semantics 保持；StripedHyena2 tiny `max_abs<0.08`；HyenaDNA `max_abs≤1e-5`；ESMC !满足 V26；否则 fail gate。
- V38: MPS model weights !跨 context 共享；mutable state !隔离；同 build+artifact+profile+input → deterministic。
- V39: `EVO_MPS=ON` 仅 Apple arm64；其他 platform → actionable configure error；`EVO_MPS=OFF` !保持 Linux/CPU/CUDA build graph。
- V40: backend dispatch !exhaustive；新 enum ⊥被既有 non-CUDA branch 当 CPU；MPS 与 `--gpu|--gpu-layers|device_count>0` 互斥。
- V41: `libevo` 链入的 ∀ CXX/CUDA/OBJCXX backend object !编译期 hidden visibility；`v41_c_api_symbols` !严格匹配 C ABI allowlist，⊥导出内部 C++/backend symbol。
- V42: MPS CI !始终编译 backend + 跑 portable contracts；`MTLCreateSystemDefaultDevice()==nil` 时仅 `v42_mps_*` hardware gates → CTest skip 77；skip ⊥作为 runtime 数值通过证据，真实 Metal host !全绿。
- V43: GENEB catalog ∀ release → `paper_name` set bit-exact = v4 Table 4 40 显示名，`geneb_model_id` set bit-exact = pinned model_meta 40 key，两集各无重复/遗漏/多余且与 40 个 `runtime_id` 双射；alias ⊥计数。
- V44: ∀ GENEB `runtime_support=supported` → canonical real checkpoint conversion+load+embed gate 存在且对 pinned input 通过独立 model oracle；只有 `benchmark_provenance=reference-eligible` 才额外要求 commit+patch official reference 在 clean locked env 产出已校验向量；catalog-only/tiny-only/source-unavailable → experimental|manual-source|unsupported，upstream-broken 可 runtime supported 但⊥ reference-eligible。
- V45: ∀ GENEB embedding → token IDs、有效 token mask、hidden tap、pooling 与所选 C21 preset 一致；oracle dtype/backend/env/`max_abs|mean_abs|cosine` 阈值 !按 model+profile 在 catalog 校准并固定，⊥将 F32 通用阈值套给 BioFM BF16 等路径。
- V46: architecture dispatch !由 artifact architecture + factory 决定；tokenizer/pooling/backend 是独立属性；⊥`tokenizer == architecture`、`bool esmc`或 unknown 默认 StripedHyena 分支。
- V47: tokenizer ∀ runtime → vocab/merges/k-mer/special IDs/normalization/padding/RC 资产在 artifact 中 strict hash+schema 校验；⊥网络、locale、Python tokenizer 或未登记 fallback。
- V48: ∀ third-party model → source revision/file hash、weight/code/tokenizer/dataset license、AUP、redistribution 状态在 receipt/catalog 分项可查；非商业/授权不明权重 ⊥进入 Git/release artifact/cache mirror。
- V49: ∀ GENEB record → 先在分配前校验 raw safety cap，再执行 I17 显式 transform；仅 `length_policy=reject` 超限才拒绝，crop/trim/pad !可重现；原序列只 materialize 1 record；输出含 model/revision/profile/preset/layer/pooling/original+effective length/crop+pad/token_count/input identity。
- V50: ∀ HyenaDNA/Mamba/StripedHyena 长上下文 checkpoint → 按模型声明上限执行且使用渐近算法；⊥静默 crop/硬限 4096/为规避实现而降低 context metadata。
- V51: ∀ model/context → immutable weights 可共享；attention/cache/SSM/conv/MoE state 隔离；同 artifact/profile/input/preset → deterministic embedding bytes。
- V52: ∀ backend capability → registry、CLI、C ABI、server/help/release metadata 一致；缺 kernel/device/memory → typed unsupported/OOM，⊥ partial output/silent CPU fallback。
- V53: ∀ GENEB runtime family → tiny deterministic converter/tokenizer/CPU/backend fixture + corruption gates；∀ 40 canonical checkpoint → pinned short-sequence independent model oracle evidence；∀ `reference-eligible` checkpoint → commit+patch official reference 在 fresh locked env 产出并通过追加 evidence。
- V54: GENEB probe → R19 dataset revision/tasks/splits/seeds/logreg 固定；`reference` 仅使用 hashed unblock patch，按 I14/I17 固定 input order、batch=8、split flush、末批不补齐与 batch-max padding，保留可执行 upstream 语义；`normalized` 逐记录消除 padding contamination/只取 batch[0] 等缺陷且只标 protocol-compatible；undefined `seq_length` 等 decision !在 T25 固定后才开工。
- V55: GENEB change 后 ∀ existing Evo2/HyenaDNA/ESMC CPU/CUDA/MPS/C ABI/CLI/server/format/fetch/release gate !green；⊥改写已验证 artifact/token/logit/embedding semantics。
- V56: ∀ promoted GENEB preset → CLI/C ABI `embed_ex`/server 的输出向量、transform/preset/shape/result-info metadata bitwise 或按 catalog tolerance parity；旧 C ABI 行为不变，互斥参数 typed fail。
- V57: GENEB full run → 每个已支持模型完整 100 tasks×`full|10shot|1shot`×`MCC|Acc|F1`，lock/submission/env digest 齐全；仅 `benchmark_provenance=reference-eligible` + reference preset + pinned official submission 可比较 per-task metric `abs≤1e-6`，normalized-only 结果只标 protocol-compatible，⊥补数/混 namespace/声称榜单复现。
- V58: ∀ embedding preset → hidden tap、CLS/mean/last/spatial pooling、special-token inclusion、mask domain 与 output width !显式固定；⊥用现有 `none|mean|last` 近似不同语义。
- V59: server `max_sequence_bytes` 是 transform/tokenizer 前 raw-record 上限且可大于 token `ctx`，但 !≤ request body cap；GENEB preset 先按 V49 校验 raw cap/transform，再要求 effective `token_count≤ctx`；⊥按 raw bytes≈tokens 提前拒绝 k-mer/BPE 合法输入。
- V60: server request/lazy-load path !在 platform worker stack 上有界执行；≥64 KiB scratch buffer !heap/arena 分配；ASan server preset smoke !完成请求且进程存活，⊥signal/crash/空 HTTP reply。
- V61: ∀ `benchmark_provenance.upstream_status=executable` GENEB row → pinned extractor 调用参数 !与 pinned model/tokenizer forward 签名实际兼容并有 clean smoke；unexpected kwarg/import/signature failure → blocked，只有显式 hashed reference patch 可恢复 reference eligibility。
- V62: ∀ GENEB input → raw safety cap 后按 I17 固定顺序执行 outer ASCII trim→case/U→T→invalid→frame trim→crop→pad；tokenizer context 截断 !只移除 payload token 并 bit-exact 保留 post-processor prefix/suffix IDs；transform/token plan metadata !报告真实移除量，⊥把被 trim 的 byte 转 N 或截掉 CLS/SEP。
- V63: ∀ production GENEB converter profile → 同一 production catalog 的 identity/source/context/tokenizer/preset !在读取任何大权重前直接交叉校验；converter contract test !使用 production catalog 而非只用由 profile 反向构造的合成条目，⊥让 tokenizer kind/特殊符号/padding/context 漂移到 real conversion 才暴露。
- V64: ∀ 影响 native 数值语义的第三方 package/tag/commit → revision !在 authoritative repository 可解析，并与锁定 release sdist/关键源文件 hash 对齐；⊥仅让 profile 与 converter 共享同一个无效 SHA 而误认为 pinned。
- V65: HF pinned snapshot receipt !逐项验证其全部文件 size/SHA/path；每个 converter !从该已验证集合要求并选择其固定 config/weight/tokenizer/remote-code 子集，缺关键文件或关键文件 evidence 不符→⊥；不因同 revision 的 README/license/未消费资产而拒绝，也不让 extra 文件影响 tensor 选择。

## §T TASKS

id|status|task|cites
T1|x|stream FASTA/raw records；移除 CLI 双重读取；保持 parser contract|V3,V5,V6,V15,V16,V17,V18,V19,I1,I5
T2|x|stream scoring chunk logits；默认⊥累计完整 logits matrix|V1,V4,V6,V15,V16,I1,I6
T3|x|抽离 backend-neutral model/context/batch；发布稳定 C ABI + install package|V1,V2,V8,V10,V14,V15,V16,I3,I8
T4|x|实现 first-class intermediate embedding API/CLI + metadata|V1,V7,V11,V15,V16,I2,I6
T5|x|实现 strand/coordinate-aware variant scoring CLI|V1,V6,V7,V12,V16,I2,I5,I6
T6|x|实现 HF revision/hash/cache 获取、CI 与预编译 release pipeline|V2,V10,V16,I2,I4,I8
T7|x|实现共享 model 的 scheduler、dynamic batching 与 bio-native server|V8,V10,V13,V16,I7
T8|x|设计并实现 fast/quantized profiles + 数值/科学 benchmark gates|V1,V7,V9,V10,V16,I4
T9|x|实现向量化 CPU backend 与显式 CPU+GPU offload policy|V2,V7,V9,V10,V16,I2,I3
T10|x|抽象 architecture registry；接入第二个生物序列模型 family|V7,V8,V10,V11,V16,I2,I3,I4
T11|x|补 FASTQ/gzip/stdin/VCF/reference IO 与坐标输出格式|V3,V5,V6,V12,V16,I5,I6
T12|x|完善贡献指南、兼容策略、benchmark matrix 与 release 文档|V7,V14,V15,V16,I8
T13|x|实现 `run|score|bench` CLI hierarchy、内建 reproducible bench 与 generation `raw|fasta` 输出|V7,V9,V10,V15,V16,V21,I1,I2,I6
T14|x|移除公共全量 sequence reader；测试与 consumer 统一 streaming API|V3,V16,V17,V20,I5
T15|x|实现 `-hf/--hf-repo REPO[@REV]` 已验证本地 cache artifact 解析|V2,V10,V16,I2,I4
T16|x|审计 production exact model ID；补真实 checkpoint raw-bit 证据或显式降级为 experimental/unsupported|V1,V10,V16,V22,I4
T17|x|核实官方 ESMC IDs/架构/tokenizer/权重/license/reference；审计扩展点并固化兼容边界|C9,C10,C12,C14,R6,R7,R8,R9,R10,R11,R12,V24,V27
T18|x|注册三尺寸；实现 pinned HF source fetch/receipt、`esmc-runtime-v1` converter/loader 与 corruption tests|C8,C9,C11,V10,V24,V28,I4,I9,I11
T19|x|实现 bit-exact protein tokenizer 与 CPU F32 forward/logits/embedding；tiny oracle tests|C10,C12,V11,V25,V26,V27,V29,V30,I10,I12
T20|x|实现单卡 CUDA F32 ESMC forward/logits/embedding 与 typed unsupported 边界|C10,C12,C13,V26,V27,V29,V30,V32,I10,I12
T21|x|接入 C API/CLI/HF offline artifact validation；补 logits/embedding metadata、能力 gate 与用户文档|C9,C12,C14,V24,V25,V27,V29,I2,I6,I10,I11,I12
T22|x|生成 pinned 官方 oracle；gpu02 验证三尺寸；跑 full regression、记录证据并清理非制品文件|C8,C10,C14,C15,V16,V26,V28,V30,V31,V34
T23|x|审计全仓 scope；修复 platform-level Evo 2-only 表述与 release metadata；增加 registry-driven contract|C7,V10,V16,V35,I8
T24|x|实现 macOS arm64 MPS backend、CLI/C ABI/server、数值与构建契约|C2,C6,C10,C14,C16,R13,R14,R15,R16,V1,V2,V7,V8,V9,V10,V14,V15,V16,V21,V26,V31,V35,V36,V37,V38,V39,V40,V41,V42,I2,I3,I6,I7,I8,I10,I12,I13
T25|x|冻结 GENEB 40-model 三 ID 双射、canonical source/file receipt、分项 license/AUP、reference/normalized extractor patch+input-transform+preset decision、oracle/env lock 与完整性 contract|C17,C18,C20,C21,C23,R17,R18,R19,R20,R21,R22,V43,V44,V45,V48,V54,V57,V58,I14,I16,I18
T26|x|重构通用 model/architecture/backend factory、registry-driven artifact profile 与 capability dispatch|C2,C3,C7,C14,C19,V1,V2,V8,V10,V14,V15,V46,V51,V52,V55,I2,I3,I4,I7,I8,I17
T27|x|实现 artifact-driven BPE/k-mer/SN/mixed/BioToken tokenizer、strict asset converter 与 catalog input-transform engine|C2,C18,C19,C21,V5,V10,V17,V25,V43,V45,V47,V49,V53,V55,V58,V62,I4,I5,I17
T28|x|实现 METAGENE、GenomeOcean×2、GENERator×2、BioFM、OmniNA 的 Llama/Mistral-derived CPU embedding/converter/oracle|C18,C19,C20,C21,C22,C23,R17,R18,V8,V10,V11,V44,V45,V48,V49,V51,V53,V55,V58,I15,I16,I17,I18
T29|x|实现 OmniDNA×2、GPT2-Gene×2、DNA-GPT×2 的 OLMo/GPT2/custom-decoder CPU embedding/converter/oracle|C18,C19,C20,C21,C22,C23,R17,R18,V8,V10,V11,V44,V45,V48,V49,V51,V53,V55,V58,I15,I16,I17,I18
T30|x|实现 NT×5 + Agro-NT 的 ESM-derived encoder CPU embedding/converter/oracle|C18,C19,C20,C21,C22,C23,R17,R18,V8,V10,V11,V44,V45,V48,V49,V51,V53,V55,V58,I15,I16,I17,I18
T31|x|实现 GENA×3、DNABERT-S/2、GROVER、MutBERT 的 BERT/Mosaic/soft-input encoder CPU embedding/converter/oracle|C18,C19,C20,C21,C22,C23,R17,R18,V8,V10,V11,V44,V45,V48,V49,V51,V53,V55,V58,I15,I16,I17,I18
T32|x|实现 LucaOne 与 Genomics-FM custom encoder CPU embedding/converter/oracle|C18,C19,C20,C21,C22,C23,R17,R18,V8,V10,V11,V44,V45,V48,V49,V51,V53,V55,V58,I15,I16,I17,I18
T33|x|实现 Evo-1-131k + HyenaDNA Medium-160k/Large-1M 长上下文原生 CPU embedding/converter/oracle|C18,C19,C20,C21,C22,C23,V1,V8,V10,V11,V44,V45,V48,V49,V50,V51,V53,V55,V58,I15,I16,I17,I18
T34|x|实现 eccDNAMamba、PlantCaduceus、Caduceus PS/PH 双向/RC Mamba 原生 CPU embedding/converter/oracle|C18,C19,C20,C21,C22,C23,V8,V10,V11,V44,V45,V48,V49,V50,V51,V53,V55,V58,V61,V63,V64,I15,I16,I17,I18
T35|x|实现 JanusDNA w/wo attention 的 Mamba+attention+MoE 原生 CPU embedding/converter/oracle|C18,C19,C20,C21,C22,C23,V8,V10,V11,V44,V45,V48,V49,V50,V51,V53,V55,V58,I15,I16,I17,I18
T36|x|实现 Enformer、SPACE 与 DeepGene pinned RoFormer-only GENEB embedding path 的原生 CPU converter/oracle；DeepGene graph stage 非 GENEB profile 不在本任务内|C18,C19,C20,C21,C22,C23,R37,R38,R39,V8,V10,V11,V44,V45,V48,V49,V51,V53,V54,V55,V58,I15,I16,I17,I18
T37|x|为有实作 kernel 与证据的 GENEB family 接入 CUDA/MPS promotion matrix、backend factory、C ABI `embed_ex`/CLI/server；其余固定 typed unsupported|C2,C6,C14,C16,C19,C22,V2,V7,V8,V9,V10,V14,V16,V36,V37,V38,V39,V40,V41,V42,V45,V51,V52,V53,V55,V56,I2,I3,I7,I8,I13,I15,I19
T38|x|实现 `models --suite geneb`、两个 embedding preset、pinned multi-source fetch/receipt 与 locked GENEB 100-task×3-regime probe runner|C8,C17,C18,C19,C20,C21,C23,V3,V5,V10,V11,V16,V17,V18,V20,V43,V44,V47,V48,V49,V52,V54,V55,V57,V58,V65,I2,I4,I5,I6,I14,I15,I16,I18
T39|x|在真实 checkpoint 上验证 CPU 40/40 conversion/load/token/embedding；验证 promoted CUDA/MPS matrix 与其余 typed unsupported；跑 full regression 并完成 docs/release evidence|C8,C15,C17,C18,C20,C22,C23,V16,V19,V22,V34,V35,V41,V42,V43,V44,V45,V48,V49,V52,V53,V54,V55,V56,V57,V58,I8,I14,I18,I19

## §B BUGS

id|date|cause|fix
B1|2026-08-12|gpu02 缺 pinned libnpy 时 build 静默退出；test 未校验 build/source 新鲜度→stale binary 被执行|V19
B2|2026-08-12|CUDA static archive 的默认可见符号绕过 shared-target visibility→C++/CUDA internals 泄漏进 C ABI DSO|V14
B3|2026-08-12|初版 C API context 仅共享 mmap artifact、重复上传 GPU weights→大模型 multi-context 显存不可扩展|V8
B4|2026-08-12|纯 CLI contract 写死 GPU 0,1；单卡 CUDA_VISIBLE_DEVICES 映射下逻辑 1 不存在→环境相关失败|V15,V16
B5|2026-08-12|GCC 8 的 std::filesystem 仍在独立 stdc++fs；streaming NPY cleanup 首次引入该依赖→link 失败|V16
B6|2026-08-12|HF fetch contract fixture 只支持空 work dir；CTest 重跑遇到既有 fake package 目录→非幂等失败|V16
B7|2026-08-12|CUDA Python test 在 source 下生成 `__pycache__`；source fingerprint 将执行产物当源码→首次 test 后误报 stale build|V16,V19
B8|2026-08-12|profile flag 在 CPU-only C ABI 中完成校验但仅由 CUDA 分支消费→`-Werror` unused-variable 破坏 CPU build|V16
B9|2026-08-12|NEON dot-product 分支后 scalar fallback 仍参与同一作用域编译，且 FIR helper 残留无效参数→Apple ARM `-Werror` build 失败|V16
B10|2026-08-12|CPU CLI 把 `std::filesystem` 引入 `evo_core`，但 GCC 8 的 `stdc++fs` 只链接到两个旧 test target→新 CPU consumer 链接失败|V16
B11|2026-08-12|新增 CPU backend 时改写 CPU-only binary 的既有 CUDA unsupported 文案→旧 CLI contract/script 失配|V15,V16
B12|2026-08-12|HyenaDNA internal header 与实现同在 `src/cpu`，include 却重复添加 `cpu/`；target 未暴露 `src` include root→T10 首次 build 失败|V16
B13|2026-08-12|sampler 用全局 Evo 2 `512` 常量校验 logits/top-k→第二个 architecture 的 16-token vocabulary 无法 generation|V7,V10,V16
B14|2026-08-12|server load gate 独立重复 `vocab == 512` 假设→HyenaDNA 已通过 registry/C ABI 仍在启动末端被拒绝|V7,V10,V16
B15|2026-08-12|C ABI sampler 测试把 Evo 2 的 512 logits 当作全局契约→动态词表实现正确后仍被旧断言判失败|V7,V10,V16
B16|2026-08-12|metadata-only C ABI fixture 未声明新增的 architecture/runtime ABI→全量 contract 在注册表校验前失败|V10,V14,V16
B17|2026-08-12|model-format fixture 增加 registry metadata 后仍写死旧 metadata count→格式解析成功却被脆弱断言判失败|V10,V16
B18|2026-08-12|HyenaDNA acceptance fixture 依赖 NumPy，但 gpu02 的可复现 Nix Python 不含该包→CUDA 门无法运行独立 oracle|V2,V16,V19
B19|2026-08-12|FASTQ quality 范围循环隐式把 signed char 转 unsigned char→AppleClang `-Werror` 拒绝构建|V16
B20|2026-08-12|VCF callback 混用显式 `Status{}` 与推导 initializer-list return，且 variant helper 参数名被反向链局部变量遮蔽→严格编译失败|V16
B21|2026-08-12|BioNeMo behavioral gate 省略 profile→model ID 降级后仍隐式请求 default exact|V23
B21|2026-08-12|gpu02 CUDA image 只有 versioned `libz.so.1` runtime、没有 zlib-devel headers/unversioned linker name→标准 FindZLIB 配置失败|V2,V16,V19
B22|2026-08-12|预先把 `EVO_ZLIB_LIBRARY` 缓存变量定义为空会让 `find_library` 视为用户已赋值而跳过搜索→本机 link 未携带 zlib|V16
B23|2026-08-12|AppleClang 未报告 lambda 参数遮蔽外层 helper 参数，GCC 8 `-Wshadow -Werror` 在 gpu02 拒绝构建|V16,V19
B24|2026-08-12|CUDA variant token-dump lambda 同样复用外层 `sequence` 名，CPU-only 本机门未编译该 TU→gpu02 才触发 GCC shadow gate|V16,V19
B25|2026-08-12|初版 ESMC 规范沿用旧 ESM SDK hidden-state convention，未逐行核对 pinned Transformers `layers_to_collect`→中间层索引整体错位|V33
B26|2026-08-12|ESMC softmax kernel 使用未由当前 include graph 暴露的 CUDA 私有 infinity macro→gpu02 CUDA 12.8 编译失败|V30
B27|2026-08-12|全量回归发现 legacy converter/documentation contracts 假设 registry.models 全是 Evo 2：新增 ESMC profile 后索引不存在的 `config` 并漏报三模型 exact 支持|V24,V31
B28|2026-08-12|ESMC 验收脚本假设 gpu02 同步目录含 `.git`，但标准 build 明确排除 VCS metadata→真实远端在运行前失败|V16,V30
B29|2026-08-12|ESMC oracle 从 receipt 首文件父目录推断 HF snapshot，但 cached symlink 返回 resolved `blobs/` 路径→官方 loader 找不到同目录制品|V28,V30
B30|2026-08-12|ESMC oracle 假设 config 暴露 `expansion_ratio`，但 pinned 实现固定 `8/3` 并向上取整至 256→真实 config 拓扑自检异常|R8,V26,V30
B31|2026-08-12|通用 Transformers 5.1 loader 把 TE 的 Safetensors `_extra_state` 当普通 attribute，无法装载官方权重→oracle 改用 PyTorch extra-state-aware `load_state_dict` 且逐 shard 核对完整 key set|R8,R12,V28,V30
B32|2026-08-12|BioNeMo 容器的可选 FlashAttention Triton RoPE 找不到无版本 `libcuda.so`→oracle 固定使用 pinned 官方 pure-PyTorch RoPE fallback 与 manual F32 attention，避免环境相关 kernel dispatch|R8,V26,V30
B33|2026-08-12|ESMC 验收脚本在 gpu02 宿主直接启动容器构建的 CUDA binary→动态链接器找不到 `libcudart.so.12`|V16,V30
B34|2026-08-12|初次 oracle 隐式采用可选 Transformer Engine fused reduction，与原生/官方 portable fallback 的顺序不同且将 hidden 微差放大为 logits 超阈值→oracle manifest 固定并校验官方 PyTorch F32 fallback|R8,V26,V30
B35|2026-08-12|ESMC gpu02 gate 默认在低配额 `$HOME` 建 HF cache，违背集群共享高容量缓存约定并触发重复下载→统一 export C15 `HF_HOME`，repo cache 固定其 `hub/` child|C15,V34
B36|2026-08-12|全量本机回归偶发让 test thread 在两次顺序 submit 间停顿超过 40ms，合法形成 2 batches 却被脆弱时序断言判失败→batch contract 使用宽窗口，取消/异常 case 隔离到 zero-window scheduler|V16
B37|2026-08-12|官方 tokenizer 对用户字面 `<pad>` 仍输出 attention-mask 1；原生 attention 仅凭 token ID 1 当自动 padding→batch-one runtime 取消隐式 ID mask，tiny oracle 纳入显式 `<pad>`|V25,V26,V30
B38|2026-08-12|共享 `HF_HOME` 已有精确 revision snapshot 且 21/21 注册 SHA256 正确，但 `huggingface_hub` 离线 API 因客户端元数据布局差异拒绝命中→`evo-fetch source --local-files-only` 对 immutable commit snapshot 直接定位 registry 文件并先验 size/SHA256；缺失或不符仍 fail closed|C15,V28,V34
B39|2026-08-12|gpu02 宿主可读共享 `HF_HOME`，但 BioNeMo Apptainer image 默认不 bind `/build/grp_icg`→ESMC fetch/conversion/oracle 容器统一显式只读 bind C15 `HF_HOME` 到同路径，避免误报 snapshot 缺失或复制权重|C15,V28,V34
B40|2026-08-12|共享 snapshot 按 B39 只读挂载后，`evo-fetch` 默认尝试在 cache child 写 receipt→新增独立 `--receipt-dir`；gpu02 gate 将 receipt 写入本次验收制品，模型 cache 全程只读|C15,V28,V34
B41|2026-08-13|ESMC benchmark contract 用 `==` 比较由二进制浮点样本计算的 median，正确聚合因表示误差失败→数值断言改用严格容差比较|V21
B42|2026-08-13|ESMC CLI 的 `evo_metrics` 按整个多记录输入聚合，benchmark 错把它当逐记录样本→CUDA logits 另发逐记录计时，summarizer 同时校验逐记录样本与单条 aggregate|V21
B43|2026-08-13|ESMC official benchmark 只同步 device logits，而原生 `prefill` 计时包含 logits 回传主机→两侧统一 `forward_with_host_logits` timing scope 并由 summarizer 拒绝混用|V21
B44|2026-08-16|`CXX_VISIBILITY_PRESET` 不作用于 OBJCXX 编译→MPS bridge 内部 C++ symbol 泄漏进 C ABI DSO|V41
B45|2026-08-16|CI 把 Apple-silicon architecture 误当 Metal GPU 可用性保证→标准 macOS runner 可能让合法 MPS hardware gate 误失败|V42
B46|2026-08-16|Evo2/HyenaDNA 已有 logits 路径未登记 `kArchitectureLogits`，旧分派又未查 capability→registry 与 C API 行为漂移；统一 V52 gate 后现形|V52
B47|2026-08-16|架构感知 `--dump-tokens` 被移到 artifact open 后→无效模型路径不再输出既有字节诊断；有效 artifact 用注册 tokenizer，open 失败保留 legacy byte dump|V15
B48|2026-08-16|installed `evo-fetch` smoke 依赖 shebang 从 `PATH` 找 `python3`→gpu02 容器已配置 Nix Python 仍失败；install contract !传 CMake pinned interpreter|V19
B49|2026-08-16|tokenizer converter 把 k-mer 专属 `k/stride` 校验放在通用 model 分支后→byte-BPE 转换触发 `KeyError`；校验归回 k-mer 分支并用全 kind matrix 回归|V10,V16,V55
B50|2026-08-16|artifact-aware CLI token dump 默认构造私有 `Status`→严格编译失败；状态显式初始化为 `Status::Ok()` 并保留 Werror syntax gate|V16,V47,V55
B51|2026-08-16|DNABERT-S 的 pinned GENEB extractor 未启用 remote code，导致内建 BERT 随机初始化未匹配 Mosaic 权重；同时目录误标 tokenizer 为 k-mer，GROVER tokenizer 又多出与 609-row embedding 不一致的不可达空 token→reference 保持 blocked，normalized runtime 固定 pinned Mosaic ALiBi+BPE，并对 GROVER 显式审计后删除 id 609|V44,V47,V53,V54
B52|2026-08-16|Genomics-FM catalog 将训练支持的 BPE+k-mer 混合能力误当作 pinned GENEB inference tokenizer，且未记录 extractor 实际省略 `use_alibi` 的 APE 路径与 `int(input_ids.device.index)` 导致的 CPU reference 异常→runtime 固定 root BPE+APE，未 patch official reference 只声明 GPU 可执行|V44,V45,V47,V53,V54,V58
B53|2026-08-16|server 将 raw `max_sequence_bytes` 强制限制为 token `ctx`→6-mer/BPE 模型在 transform/tokenizer 前误拒绝可编码到合法上下文的输入|V17,V49,V59
B54|2026-08-16|tokenizer asset hash 在栈上分配 1 MiB buffer；server 首请求的 lazy CPU load 运行于小栈 worker→macOS pointer-auth/ASan stack overflow、空 HTTP reply|V13,V16,V60
B55|2026-08-16|LucaOne catalog 用训练/remote tokenizer 的 mixed 能力描述 pinned GENEB extractor，但 extractor 固定 `gene` modality 且逐字符映射→runtime 固定 gene character tokenizer；protein/multi-sequence 模式不属于该 profile|V44,V45,V47,V53,V58
B56|2026-08-16|GENEB Caduceus extractor 把 tokenizer 返回的 `attention_mask` 传给三条 pinned remote model，但其 `forward` 均不接受该参数→clean reference 直接 `TypeError`；无 hashed patch 时 reference/upstream 必须 blocked|V44,V53,V54,V61
B57|2026-08-16|Genomics-FM pinned extractor 在 upper/invalid-normalization 前执行 `strip()`，catalog/engine 未表达 outer trim→首尾 ASCII 空白被误转为 N|V62
B58|2026-08-16|shared GENEB planner 在 tokenizer 已追加 CLS/SEP 后对整个 token vector 右截断→LucaOne/Genomics-FM 超长输入丢失 pinned 尾 SEP|V62
B59|2026-08-16|Genomics-FM converter 从源码命名推断 MLM bias 为 `cls.predictions.bias`，但真实 pinned checkpoint 键为 `cls.predictions.decoder.bias`→V53 exact source-manifest gate 在 real conversion 前失败|V53
B60|2026-08-16|两条 pinned HyenaDNA tokenizer 固定 left padding，但 catalog 误记为 right；因 causal Hyena forward 会让 pad token 参与隐藏态计算，方向漂移会改变短序列 embedding|V45,V53
B61|2026-08-16|JanusDNA pinned GENEB wrapper 默认 CPU，但上游未提供 config override 且 constructor 默认启用仅 CUDA 可用的 Mamba fast kernels→clean official CPU reference 运行前即失败；catalog upstream/reference 必须 blocked，native CPU 只能作为独立 oracle 的 normalized profile|V44,V53,V54
B62|2026-08-17|PlantCaduceus production converter profile/已编译 tokenizer 依 pinned HF `tokenizer.json` 固定 BPE，但 production catalog 仍记 `single-nucleotide`；现有 converter test 反向构造合成 catalog 而掩盖漂移→real conversion 在权重读取前必然 fail closed|V45,V53,V55,V63
B63|2026-08-17|eccDNAMamba profile/converter 对 `mamba-ssm 2.2.4` 共用同一个无法解析的 typo commit `95d8aba86c…`，所以内部 exact-equality gate 绿仍不代表 provenance 已 pin；official tag 实为 `95d8aba8a8c75aedcaa6143713b11e745e7cd0d9`|V48,V53,V64
B64|2026-08-17|GENEB HF fetch 在 catalog 未列 required_files 时验证并收据化整个 pinned snapshot，但 family converter 又要求 receipt 文件集合与消费子集完全相等→标准 fetch 产生的合法 README/license/额外资产必被 real conversion 拒绝；converter 改为先验全 receipt integrity、再固定选择 profile 子集|V48,V53,V55,V65
B65|2026-08-17|GENEB architecture docs/release metadata 未机检声明 backend/capability 与实际 factory/public surfaces 一致，导致前四个 CPU 架构漏写 serve，且无 factory 的 CUDA/MPS 仍以含糊 not-promoted 暴露|V52
B66|2026-08-17|CLI/server 默认 `--ctx=8192` 未区分用户显式请求；对 max context<8192 的合法 GENEB artifact，在读取短输入前即按超上限拒绝，导致默认 embed/serve 不可用|V49,V52,V55
B67|2026-08-17|公共 embedding 适配器沿用 Transformer 假设，把内部 block 深度当可捕获层数并强制 hidden rows=token rows；Enformer/SPACE 实际只公开最终 896 行序列嵌入，且固定 131072/196608 输入会同时越过默认 context→SequenceCNN 固定 public layer0，preset 按返回矩阵行数做 spatial mean，默认 CLI/server 选择固定输入容量，显式小容量仍拒绝|V49,V52,V55,V56
B68|2026-08-17|tokenizer converter 把新版 tokenizers JSON 的显式默认 `byte_fallback=false` 当 BPE 必需字段；DNABERT-2 pinned 旧格式省略该默认值，导致真实 tokenizer 在编译前误拒绝→缺失与显式 false 归一为同一关闭语义，显式 true 继续 fail closed|V10,V47,V53,V55
B69|2026-08-17|HF fetch receipt 以 canonical `name=pytorch_model.bin` 映射到 content-addressed blob path；共享 reader 又按 physical basename 二次拒绝，导致已验 SHA/size 的真实 cache checkpoint 无法转换→checkpoint-set loader 以已验证 logical name 决定格式，direct unverified helper 仍保留 basename gate|V10,V47,V48,V53,V55,V65
B70|2026-08-17|independent-oracle vector 的 environment/provenance 可携带 `/private/tmp` 等机器绝对路径，runner 会将其原样写入仓库 evidence，导致跨机证据不稳定→递归拒绝本地 absolute filesystem path，证据仅保留版本、revision、内容 digest 与语义标识|V53,V55
B71|2026-08-17|GENEB fetch receipt/artifact 绑定整个可变 catalog 文件 SHA；任一模型 promotion 会使其余已验 receipt 失效，并让同一 checkpoint 在最终 catalog 下产生不同 artifact SHA，已记录 evidence 无法重现→来源、converter、artifact 与 evidence 统一绑定排除 `oracle/runtime_support/backends/promotion_state` 的稳定 per-model catalog contract digest；改变 source/tokenizer/context 必须拒绝|V48,V53,V55
B72|2026-08-17|HF source fetch 忽略 catalog `required_files` 并无界下载整个 pinned repo；GROVER 因而连同 18.17GB `tokenized_chromosomes/` 训练语料下载，既非运行时输入又可能耗尽磁盘→存在 nonempty allowlist 时只能下载/receipt 这些 conversion-critical 路径；无 allowlist 的全仓发现必须有显式策略与总量安全 cap，训练数据不进入 checkpoint evidence|V47,V48,V53,V55,V65
B73|2026-08-17|GROVER pinned tokenizer 的空 vocab piece `id=609` 还被前四条字面 merge `A␠/C␠/G␠/T␠` 引用；只删空 entry 会令 strict compiler 拒绝，且 catalog 未证据化 tokenizer patch→同时精确删除这四条永不可达、输出恒等的 merge，并以官方 tokenizer token parity gate 锁定|V10,V44,V47,V53,V55
B74|2026-08-17|真实 checkpoint evidence 将成功路径 stderr 中的 `model_load_seconds` 等非确定 telemetry 和 embedding manifest 的输入绝对路径纳入摘要；同一 receipt/artifact/oracle/output 二次运行仅因耗时/机器路径产生不同 evidence SHA→成功证据规范化 timing 字段与输入名为内容标识，失败诊断仍保留稳定摘要，并以跨路径重复 runner 的逐字节一致性 gate 锁定|V53,V55
B75|2026-08-17|共享 HF PyTorch reader 把 `pathlib.Path` 直接传给 `torch.load(..., mmap=True)`；新版本接受但 pinned oracle/converter 环境的 PyTorch 2.1 要求字符串文件名，真实 DNABERT-2 转换在安全加载前失败→verified path 在调用边界显式转为 `str`，并以 content-addressed blob + PyTorch 2.1 real-load gate 锁定|V48,V53,V55
B76|2026-08-17|GENEB catalog contract 测试把 T25/T37 的 `supported=0`、CPU 全 `not-promoted` 临时里程碑写成永久断言；首批 T39 真实 checkpoint evidence 正确 promotion 后反而全量回归失败→按行校验未 promotion 与 canonical evidence-backed CPU promotion 两种合法状态，validator summary 必须与 catalog 支持计数一致|V53,V55
B77|2026-08-17|tokenizer descriptor 以原始 source receipt bytes 计算 `source_receipt_sha256`，而 receipt 的 locator `files[].path` 是机器绝对路径；同一 tokenizer 内容换 cache 根目录会改变 descriptor，并经 `source.tokenizer_descriptor_sha256` 改变模型 artifact/evidence SHA→先验证真实 locator，再对仅含 schema/kind/role/name/size/content-SHA 的规范投影计算 `source_receipt_contract_sha256`，跨绝对路径编译必须逐字节一致|V47,V48,V53,V55
B78|2026-08-17|GENA pinned tokenizer 使用 tokenizers 旧 schema 的 BPE decoder discriminator `type="BPE"`，严格 compiler 只接受同义新版 `BPEDecoder`，真实 tokenizer 在不参与 encode 的 decoder 字段误拒绝→仅把 exact `{type:BPE|BPEDecoder,suffix}` 归一为同一已审计语义，未知字段/其他 decoder 继续 fail closed，并锁定两别名产物逐字节一致|V10,V47,V53,V55
B79|2026-08-17|T36 PyTorch checkpoint reader 把 `pathlib.Path` 直接传给 `torch.load(..., mmap=True)`；PyTorch 2.1 要求具体字符串文件名，导致真实 DeepGene/Enformer/SPACE 在安全加载前失败而合成测试仍绿→receipt 验证后的每个 mmap checkpoint 边界显式传 `str(path)`，并以 filename-type gate 和真实 PyTorch 2.1 conversion 锁定|V48,V53,V55
B80|2026-08-17|DNABERT-S pinned `tokenizer.json` 保留训练期 backend truncation/padding=2000，而 GENEB 每次调用显式 `truncation=True,max_length=512,padding=True` 覆盖该 backend 状态；strict compiler 既误拒绝 truncation 又会错误固化 padding 2000→只有模型 manifest 显式声明 extractor call 覆盖时才验证并忽略 exact source backend length state，未 opt-in/未知结构继续 fail closed，并以官方 HF/GENEB token parity 与 B77 跨路径门锁定|V10,V44,V47,V53,V55
B81|2026-08-17|DeepGene catalog/runtime 明确要求 token 顺序 `[CLS],[SEP],payload`，但通用 GENEB planner 把该 policy 当普通 tokenizer template，实际生成 `[CLS],payload,[SEP]`；tiny typed runtime 又直接喂正确顺序而掩盖 public adapter 漂移→manual-CLS-then-SEP policy 只接受单 prefix+单 suffix 的 audited tokenizer template，并在 truncation/padding 前规范重排为双前缀；public prepare gate锁定 exact token order|V44,V45,V53,V55,V56
B82|2026-08-17|checkpoint evidence runner 把所有 `source-checkpoint` 当 Hugging Face，并把 manual receipt 唯一特判写死为 JanusDNA；DeepGene、Genomics-FM 与 DNA-GPT 的逐文件 SHA receipt 因而无法进入 promotion 链→manual source 按 catalog `kind/url/immutable/required_files` 通用验证，唯一 basename 映射兼容 converter 的逻辑文件名，Janus legacy kind 仅作兼容别名，未知/额外/缺失来源仍 fail closed|V48,V53,V55
B83|2026-08-17|DeepGene 首个真实 oracle 沿用未经该模型校准的 `mean_abs=1e-6` 临时门，正确 F32 scalar RoFormer 在两条固定输入上分别为 `1.26497e-6/8.79399e-7`，导致 max/cosine 均通过仍被误判→冻结该模型经双输入校准的 `max_abs=1e-5,mean_abs=2e-6,cosine=0.999999999`，不得作为其他模型的通用阈值|V45,V53,V55
B84|2026-08-17|DeepGene real conversion/profile/receipt 已锁定 config 与 checkpoint SHA，但 catalog manual `required_files` 仍保留 null；evidence promotion 因而让目录自身无法证明所支持的手工文件集合→manual model promotion 前必须把 receipt 已验证的 exact size/SHA 回写 catalog，随后按新 catalog contract 重转 artifact 与 evidence；catalog contract test拒绝任何 promoted manual null pin|V48,V53,V55
B85|2026-08-17|NT-v2-50M-3mer-MS pinned config 含两次同值 `attention_probs_dropout_prob: 0.0`；通用 duplicate-key 拒绝器正确 fail closed，但官方 Transformers 按 last-wins 可加载，导致真实 checkpoint conversion 被合成 fixture 掩盖→仅在完整 config SHA 已匹配的该 profile 中允许该键精确出现两次且两值均为数值 0.0，其他重复键、计数或异值继续拒绝，并以 production bytes 与 divergent-duplicate corruption gate 锁定|V10,V48,V53,V55
B86|2026-08-17|NT-v2-50M-3mer-MS PyTorch checkpoint 的 73 个二维权重是覆盖完整 storage 的精确列主序 view（stride=`(1,rows)`, offset=0）；通用 reader 正确拒绝任意 non-contiguous tensor，却令官方逻辑值无法转换→仅由该 profile 显式启用 `contiguous-or-exact-column-major`，且只接受 rank-2、offset=0、full-storage、精确 stride 的 view 后按逻辑索引物化 row-major artifact；任意其他 stride/offset/overlap 默认与 opt-in 路径均拒绝，并锁定 default-reject、production manifest 和 corruption gates|V10,V48,V53,V55
B87|2026-08-17|GENEB 多模型 converter 把整个 profile 文件的 SHA 写入单模型 metadata；修改任一 sibling 行会无语义地改变已 promoted 模型 artifact/evidence，重现 B71 的跨模型耦合（命中 decoder/BERT/custom/GPT2/DNA-GPT/OLMo/ESM/Mamba/Janus/HyenaDNA/SequenceCNN）→先验证完整 profile 文件，再统一只对 `{schema_version,format,implementation_contract(s),selected_model}` 的 canonical 投影计算 `source.converter_profile_contract_sha256`；其他模型行变更必须保持 artifact byte-identical，本模型 source/topology/tokenizer 变更必须改变或拒绝，并一次性重签实际受影响 evidence；单行 Evo1/RoFormer 也使用同一 helper 防未来扩展|V48,V53,V55
B88|2026-08-17|checkpoint evidence runner 强制所有模型提供 artifact tokenizer descriptor，并给所有 converter 注入 tokenizer 参数；Enformer/SPACE 的输入是 catalog 锁定的 crop/pad/one-hot transform，artifact 有意不含 tokenizer asset，故 Sequence-CNN 真实证据无法运行→evidence tokenizer 改为封闭 union：既有 descriptor 形态保持兼容，`asset_source=input-transform` 时只接受无 descriptor/root、记录 tokenizer+input-transform canonical contract 与物理 vocab，并要求 artifact 无 `tokenizer.*`/descriptor metadata且 catalog contract一致；converter 参数按该 union 精确注入，未知/混合形态拒绝|V44,V47,V48,V53,V55
B89|2026-08-17|MutBERT extractor 仅传 `truncation=True` 而 pinned tokenizer 没有 `model_max_length`，HF 将其视为无限 sentinel 并实际不截断；catalog/runtime 却把 512 当 reference tokenizer cap，长输入会伪称 parity→reference contract 明确只对固定短输入可比、`reference_max_tokens=null` 且记录上游无截断缺陷；normalized profile 保留模型安全 cap 512，增加 >512 differential gate，任何 promotion 不得宣称 reference 长输入一致|V44,V45,V49,V53,V55
B90|2026-08-17|HF tokenizer 的 added special token 若已以相同 piece/id 存在基础 vocab，compiler 会跳过追加并同时漏掉 literal matcher；MutBERT 因此把字面 `[MASK]` 当普通字符流，虽短 benchmark 输入未触发仍不具完整 tokenizer parity→所有通过 AddedToken flags/collision/数量/normalizer 审计的 special literal 都进入 `literal_token_ids`，无论 ID 是 appended 或 base-vocab exact alias；异 ID/异 piece/不支持 flags 继续拒绝，并以 `[MASK]A` 官方/native exact vector及 corruption gate 锁定|V10,V47,V53,V55
B91|2026-08-17|共享 GENEB mean pooling 先在 F32 计算 reciprocal 再逐元素 multiply，而 pinned PyTorch/NumPy 对逐行 F32 sum 直接执行 `/ count`；对 GENA 的 11 个 token，仅该舍入边界即可产生 `3.0517578125e-5` 漂移并掩盖已 bit-exact 的 hidden states→masked/all-row mean 固定为相同顺序 F32 sum 后 F32 division，禁止 reciprocal-multiply 代换；以 11-row cancellation 向量和真实 GENA hidden→pool 门锁定，并重签实际受影响模型证据|V45,V53,V55,V56
B92|2026-08-17|B90 将 base-vocab AddedToken special alias 全部编译为 literal matcher，但 PlantCaduceus 的 pinned tokenizer 同时有 `Lowercase` normalizer 与 `[PAD]/[MASK]/[UNK]` 的 `normalized=false` base aliases；现有 runtime 在 literal matching 前归一化，既无法保真这些 raw literals又令已 promoted 的 DNA-domain asset 无法重编→仅由模型 manifest 显式启用 `ignore_hf_base_vocab_added_special_literals`，严格审计并舍弃 exact base-vocab alias（appended special、异 ID/piece/flags、未知 normalizer 仍拒绝）；Plant 唯一 opt-in并锁定旧 asset SHA、官方 DNA token parity 与其他 B90 tokenizer 的 literal parity|V10,V47,V53,V55
B93|2026-08-17|Custom Encoder 的私有 attention-mask mean 仍以 F32 reciprocal-multiply 实现，而 B91 已把公共 GENEB/pinned NumPy 语义固定为逐列 sum 后 direct division；Release 优化偶然合并两式，ASan/O0 则暴露 1 ULP public/private adapter 漂移→该 family pool 同步改为相同顺序的 direct F32 division，并以 Release+ASan converter→public adapter 与独立 NumPy vectors 锁定|V45,V53,V55,V56
B94|2026-08-17|GENA-LM-T2T-Multi 的 pinned HF tokenizer 同时使用 `Strip/Replace/Split` normalizer 与 `normalized=false` 的 base-vocab AddedToken special aliases；B90 的 literal matcher 只允许空 normalizer，而 runtime 又先整体归一化，故真实 `[MASK]` 会在编译前被拒或被错误改写→严格审计的 `normalized=false,special=true` literal 必须在 raw input 上先做最长匹配并隔离，literal 直接发固定 ID、其余 gap 各自执行既有 normalizer→pretokenizer→BPE；Plant 的 B92 显式忽略路径保持原 asset，冲突/flags/数量/未知 normalizer 继续 fail closed，并以 GENA 真实 tokenizer 官方/native parity、跨路径 B77 与 corruption gate 锁定|V10,V44,V47,V53,V55
B95|2026-08-17|NT-v2-250M-MS 的 pinned PyTorch checkpoint 含且仅含 145 个覆盖完整 storage 的二维列主序权重 view（F32、offset=0、stride=`(1,rows)`）；该 profile 未声明 B86 已冻结的显式布局 opt-in，故通用 reader 正确 fail closed 而真实转换无法继续→仅给该 exact profile 增加 `contiguous-or-exact-column-major`，复用 B86 的 rank/offset/full-storage/stride 白名单物化逻辑；默认 reader、任意其他 layout 与未声明模型继续拒绝，并以真实 145-tensor manifest、双路径 artifact/evidence 与 corruption gate 锁定|V10,V48,V53,V55
B96|2026-08-17|BioFM pinned `AnnotationTokenizer` 默认 left padding，但 GENEB `Embedder` 丢弃 attention mask，并把 `valid_count-2` 当绝对行号取末 payload hidden；batch 内短记录因此既受 causal pad 污染又取错行，catalog 还误写 right padding且公共 preset只有不可执行的泛化 `model-defined-sequence-pooling`→clean batched reference保持 blocked并量化差分；normalized contract 固定逐记录无 pad（等价 right/no-pad）与显式 `penultimate-valid-token` 池化，tokenizer锁 `[CLS],payload,[SEP]`、model-vocab512/tokenizer-vocab24及真实 unknown/special IDs，以官方单记录、污染 batch、converter→public adapter和corruption门锁定|V44,V45,V47,V53,V55,V56
B97|2026-08-17|OmniNA pinned BPE 同时声明无 byte piece 的 `byte_fallback=true`、DNA域不可达但对重复未知 Unicode 生效的 `fuse_unk=true`、decode-only Sequence decoder、normalized=true base aliases和 normalized=false appended `[PAD]`；GENEB 又把 pad 改成 EOS id2/left padding，且无限 tokenizer sentinel令长输入不截断，而 catalog/runtime误写 right/id32000与2048 reference cap→仅由该 exact manifest审计并允许：零 byte-piece+非空 unk且固定DNA域时忽略 byte fallback/fuse_unk、只接受固定 decode-only decoder、显式忽略 normalized=true base aliases但保留 appended raw literal的 B94 matcher；catalog锁 left/EOS padding、`reference_max_tokens=null`及>2048/重复未知差分，normalized逐记录cap2048，短DNA官方/native token与数值门不得宣称一般Unicode、长输入或批量 reference parity|V10,V44,V45,V47,V49,V53,V55
B98|2026-08-17|OmniNA 的 checkpoint-era Transformers 4.29.2 `LlamaTokenizerFast` 会产出 LlamaModel 不接受的 `token_type_ids`，但 GENEB 自身 `uv.lock` 固定 Transformers 4.57.3/tokenizers 0.22.2，在该精确环境中 `model_input_names` 与 encoding 仅含 `input_ids+attention_mask`，clean extractor 可执行；若沿用旧环境会把历史 TypeError 误报成当前 reference 缺陷→oracle 与 promotion 必须以 GENEB locked 4.57.3 clean call 为准并保存旧4.29.2 differential；不得加入无依据的 drop-key patch，未来依赖漂移若重新产生不兼容键则按 V61 fail closed|V44,V45,V53,V55,V61
B99|2026-08-17|GenomeOcean-500M 的 layer-0 embedding 与 official Transformers 4.38.2 CPU-F32 bitwise exact，但 profile 把 `activation_dtype` 误锁为 BF16；native 从 layer-1 起逐边界量化（首层 max `1.5294e-3`），而 pinned GENEB CPU extractor 显式以 F32 加载/执行，最终超门至 max `5.1083922e-2`、mean `5.3816369e-3`、cosine `0.99992931`→仅该 profile 将 activation dtype 固定为 F32，source embedding/projection/norm BF16 权重仍按位展开；禁止放宽容差，用同一 token/oracle 与 final native SHA 重转并跑双路径 evidence，且 decoder dtype corruption/tiny vectors与既有 evidence必须回归|V44,V48,V53,V55,V56
B100|2026-08-17|BioFM-265M 的真实 tokenizer、artifact load 与 `penultimate-valid-token` 已闭合，clean batch 缺陷也独立复现，但首个 single-record official Transformers 4.50/Torch 2.5.1 CPU-BF16 oracle 相对 native 仍超冻结门（max `6.25e-2`、mean `2.9590e-3`、cosine `0.99998047`）→禁止把 failed evidence promotion 或放宽容差；先证明选择行一致并逐层定位首个 BF16 数值边界，随后把修复限制到 BioFM/decoder 的真实 upstream BF16 运算语义，用同一 artifact/token/oracle/final-native 双路径复验，且既有 decoder BF16/F32 vectors与 evidence必须全回归|V44,V45,V48,V53,V55,V56
B101|2026-08-17|Agro-NT-1B 的 source.required_files 为空会让 bounded HF fetch 同时下载未被 GENEB/converter/oracle执行的 3.69GiB JAX checkpoint，使真实取证峰值从约7.36GiB膨胀到11.05GiB且逼近磁盘耗尽；pinned config又无 auto_map，repo remote-code文件实际不会被 Transformers 导入→catalog 固定仅含 config、PyTorch checkpoint与三份 tokenizer文件的5项 size/SHA allowlist，明确排除JAX/README/未执行remote code；oracle锁环境内建 Transformers ESM源码，converter与独立oracle逐项消费已验证receipt，并用串行双artifact避免无必要峰值|V48,V53,V55,V65
B102|2026-08-17|Agro-NT-1B 的真实 PyTorch checkpoint 含242个 exact二维全storage列主序矩阵而 profile 未声明B86布局opt-in；同时 pinned vocab.txt 有4107行，但 config/embedding只有4105，尾部 `<eos>`/`<bos>` id4105/4106可由官方 tokenizer 字面触发并导致模型OOB，普通DNA只发0..4104→仅该profile启用 `contiguous-or-exact-column-major` 并锁652/242真实manifest；专属 tokenizer compiler 必须验证完整vocab文件后只发布 exact 0..4104，逐字审计尾部4105=`<eos>`/4106=`<bos>`且将其标为输入域外拒绝，任何其他计数/顺序/内容或运行时越界ID fail closed；以普通DNA官方/native parity、两个尾部literal拒绝、双路径artifact/evidence与layout corruption门锁定|V10,V44,V47,V48,V53,V55
B103|2026-08-17|OmniNA-220M 真实 checkpoint 额外保存16个逐层 `self_attn.rotary_emb.inv_freq` BF16 buffer；现行 Transformers 与 native 均从 config 动态构造 RoPE、但源 buffer 在第7项相对当前公式差1 BF16 ULP，既不能消费也不能泛称公式等价，strict manifest 因而正确拒绝→仅该 profile 显式 `legacy_rotary_inv_freq` validate-and-omit，锁定层 ordinal 0..15、名称、shape `[32]`、BF16、数量16及每张量内容 SHA `8743397b60ff5e2c99e396f6342c654eb02f988fd35b6c725d6a5fc341dbed6d`；缺失/额外/异值/异dtype/shape均拒绝，其他 decoder profile 继续 exact manifest|V10,V48,V53,V55
B104|2026-08-17|BioFM B100 首差定位为 pinned Torch 2.5.1 CPU-BF16 SDPA 选择 CPU Flash 边界，而通用 decoder 只实现 eager softmax；上游 Flash 保留 F32 QK/scale/max/sum，把未归一 exponential 写入 BF16 scratch，用 F32 累加 value 后 direct division 并回写 BF16，其 BLAS/vector-exp 归约次序又存在 host 差异→artifact 新增可选 `decoder.attention_kernel=torch-cpu-flash-bf16-portable`，仅 BioFM 显式启用且要求 BF16+full-causal，旧/其他 profile 继续 eager；两条固定 exact-SDPA 上游向量分别得 max `6.25e-2/3.125e-2`、mean `2.5319025e-3/3.3996329e-3`、cosine `0.99998421/0.99997907`，因而仅该 portable BF16 profile 冻结 `max_abs≤0.125,mean_abs≤0.005,cosine≥0.99997`，不外推为 F32/其他模型通用门，并以新旧kernel差分、非BF16拒绝、双根artifact/evidence与全decoder回归锁定|V44,V45,V48,V53,V55,V56
B105|2026-08-17|Omni-DNA-1B catalog 未冻结 required_files 且遗漏已可确定编译的 tokenizer manifest；B72 全仓发现会引入不执行的 README/.gitattributes/fast-tokenizer 而增大 4.33GB checkpoint 取证峰值，也使同一 pinned source 缺少闭合 tokenizer descriptor→该行只 receipt config、model.safetensors、`configuration_olmo.py`、`modeling_olmo.py`、special-token map、tokenizer.json/tokenizer_config 七项 exact size/SHA，明确排除非执行资产；仓库纳入专属 compiler manifest 并锁定 B90 后 asset SHA/size，要求与已 promotion Omni-DNA-300M 的 source-tokenizer 内容等价但不复用其来源证据；真实转换在至少9GiB空间下串行两根，cmp后立即删除重复 artifact|V10,V47,V48,V53,V55,V65
B106|2026-08-17|Omni-DNA remote model 依赖 `ai2-olmo==0.6.0`，其顶层 `olmo.util` 会无条件 import `datasets`，但 base package metadata 只在 train extra 声明该依赖；按 model card 仅安装 `ai2-olmo` 的隔离 oracle 因而在权重执行前 `ModuleNotFoundError`，合成/已装全环境会掩盖此 provenance 漂移→独立 OLMo oracle 必须显式锁 `datasets==2.20.0`（与已 promotion Omni-DNA-300M 的冻结环境一致且满足 GENEB lock），完整记录 transitive package lock；不得用未锁最新版、静默复用全局环境或把 packaging 修补冒充数值 runtime 语义|V44,V53,V55
B107|2026-08-17|Omni-DNA-1B 的14-token真实门在 tokenizer、artifact与cosine均正确时仍超至 max `3.0517578125e-5`、mean `1.3261640461337265e-6`；追查时同时发现 OLMo typed runtime 的私有 attention-mask mean 仍以 reciprocal-multiply 实现，但 CLI/C API preset 实际走已修复的公共 direct-division pool，修正私有路径后真实输出逐位不变→保留私有 pool 与 B91 的 direct F32 division 同步及可区分回归，但不得把它冒充超门根因；Omni-DNA-1B 继续以冻结双输入做逐层首分歧定位且禁止放宽门，以既有 Omni-DNA-300M、Release+ASan typed/vector/converter→public adapter锁定无旁路漂移|V45,V53,V55,V56
B108|2026-08-17|OmniNA-220M 在 tokenizer、embedding与pooling均一致后仍从首个 decoder block 超 F32 门；逐算子定位证明 pinned GENEB uv.lock Torch 2.7.1 Apple-arm64 CPU 使用 Accelerate SGEMM、固定 cascade-sum RMSNorm、split-half RoPE 的 SLEEF sin/cos、CPU-Flash 的固定 q/kv 分块与4-lane SLEEF exp/NEON归约，以及4-lane SLEEF SiLU，而通用 scalar 累加从 RMS/QKV 起放大到最终 mean `2.422e-6`→artifact 仅为 OmniNA 显式选择 `decoder.f32_math_kernel=torch-2.7.1-apple-arm64-exact-v1`；新增隔离 TU 复刻该闭合算子顺序，SLEEF 派生自 PyTorch v2.7.1 submodule `shibatch/sleef@56e1f79cb140fb9326d612d0be06b5250565cade` 并保留 Boost-1.0 notice，局部禁用 FP contraction且不得链接/加载 libtorch；非 Apple-arm64 对该 profile 在 forward 前 typed unsupported，其他 profile 继续既有 kernel/bytes；以两条端到端 bit-exact 向量、block-0 全算子 capture、metadata corruption、Release+ASan 与既有 decoder/evidence回归锁定|V44,V45,V48,V53,V55,V56
B109|2026-08-17|B91/B93 修复公共与 Custom mean 后，全局审计仍发现 ESM、GPT/DNA-GPT、Evo1、HyenaDNA 与 Mamba typed API 的私有 pooled result 使用 reciprocal-multiply；公共 preset 多数从 capture 另行池化，令真实 evidence 可绿却留下 typed/public 与 Release/ASan 潜在1 ULP分叉→所有 mean 实现统一为相同顺序 F32 sum 后 direct division，禁止 family 私有旁路；以公共/可构造路径的可区分向量、各family exact direct-result断言、Release+ASan typed/vector/converter→public adapter及既有 evidence回归锁定|V45,V53,V55,V56
B110|2026-08-17|Omni-DNA-1B 在 B107 排除 pooling 后确认 layer-0 embedding 28,672个F32逐位一致，首个 block 的无仿射 LayerNorm 即分叉（max `2.6702880859375e-5`、mean `1.433674646302206e-7`），并累积到 layer-16 hidden mean `2.6341783421836496e-6` 与最终超门；native sequential mean/variance不等于 pinned Torch 2.1.2 Apple-arm64 `F.layer_norm` reduction→禁止放宽门；artifact仅由 Omni-DNA-1B 显式选择 `olmo.layer_norm_kernel=torch-2.1.2-apple-arm64-exact-v1`，实现8-lane/16-chunk Welford-cascade moments及等价 affine form `x*rstd+(-mean*rstd)`，并锁 layernorm-no-affine/width2048/L16/H16/FF16384/F32/eps1e-5闭合拓扑；非Apple-arm64该profile forward前typed unsupported，tiny与Omni-DNA-300M无键继续portable；以 bit-exact LayerNorm算子、原双输入（max分别`7.62939453125e-6/3.814697265625e-6`）、Release+ASan及300M回归锁定|V44,V45,V53,V55,V56
B111|2026-08-17|OLMo converter 的 CPU-adapter closure 把 synthetic character tokenizer 的 `ACG→[1,4,2]` 硬编码成全 profile 真值；真实 Omni-DNA B90 BPE 正确编码为 `[1,5,3206,2]`，导致 artifact tokenizer/public adapter实际成功却被测试误报失败→adapter gate按 artifact `geneb.runtime_id` 选择封闭的 synthetic或两条production Omni token向量，并将实际token/rows写入结果；未知 runtime ID拒绝，以 synthetic converter与真实1B/300M tokenizer closure锁定|V10,V47,V53,V55,V56
B112|2026-08-17|Agro-NT-1B 的 pinned built-in ESM oracle、4105-vocab tokenizer、3.93GB artifact 与 final public CPU adapter均严格通过，但首条1024-token门超出预冻结阈值；layer-0有效embedding逐位一致，首个物质分歧为第0层 attention LayerNorm，native two-pass 与 pinned Torch2.1.2 Apple-arm64 RowwiseMoments/仿射顺序相差 max `4.7683716e-7`，隔离复刻8-lane/16-chunk cascade及 `(x*rstd+(-mean*rstd))*weight+bias` 后全1024×1500逐位一致→artifact仅Agro显式选择 `esm.layer_norm_kernel=torch-2.1.2-apple-arm64-exact-v1` 并锁 width1500/F32/eps1e-12/affine拓扑；非Apple-arm64 forward前typed unsupported，ESM siblings无键继续portable；禁止放宽原门，以独立bit-vector、metadata corruption、双根artifact/evidence与全ESM Release+ASan回归锁定|V44,V45,V48,V53,V55,V56
B113|2026-08-17|LucaOne pinned revision `f0d2807e…` 的第二Safetensors shard在官方 LFS pointer、B72 receipt与完整本地SHA重算均为 size `1390366196`/SHA `1b2b9ec5…c5fb5`，但 custom-encoder profile误锁不存在的 `e5091188…f265`；同时 catalog `required_files=null` 未绑定 converter 已冻结的13项来源闭集→以官方固定revision真实值替换错误profile常量，catalog显式列出同13项 exact name/size/SHA并重取B71 receipt；缺失/额外/旧SHA均fail closed，真实双shard转换前必须先通过profile↔receipt全项相等与production source lock|V10,V48,V53,V65
B114|2026-08-17|GENERATOR 的 pinned remote tokenizer并非 plain `vocab-text`：`tokenizer.py` 按每行 `piece id` 读取4128个连续ID，而通用 vocab-text compiler会把整行误当piece；GENEB又先拼字面`<s>`并由 tokenizer追加BOS，形成可验证的double-BOS，强套现有格式会静默改变IDs→为GENERATOR profile新增独立、hash锁定的 indexed-vocab→`evo-tokenizer-source-v1` 生成器，严格审计 tokenizer.py/config/special map/vocab的行语法、连续ID、32个special literal及全文件SHA，再复用既有 custom/kmer k6/stride6/tail-lookup/match-special-literals compiler；catalog绑定7项执行allowlist与compiler manifest，缺项/乱序/重复ID/flags/literal漂移均拒绝，并以官方4.57.3双输入double-BOS parity、跨根asset/descriptor及converter→public adapter锁定|V10,V44,V47,V48,V53,V65
B115|2026-08-17|NT-2.5B-MS pinned HF revision同时发布 PyTorch `10.19GB`、TensorFlow `10.19GB` 与 JAX `9.49GB` 权重，总计 `29,869,949,420` bytes；catalog `required_files=null` 会被B72 16GiB预检拒绝，放宽上限则会下载两套不执行权重→source allowlist仅保留 config、PyTorch双shard+index、special-token map、tokenizer config与vocab七项 exact size/SHA，明确排除JAX/TF/README/.gitattributes；receipt必须先绑定B71 catalog contract且 converter/oracle只从verified七项取值，缺失/额外选取/格式混用均fail closed|V10,V48,V53,V65
B116|2026-08-17|GenomeOcean-4B 的 pinned repo 已解析出执行所需 config、Safetensors双shard+index与三项tokenizer文件，但 catalog仍为 `required_files=null`/`tokenizer.assets=null`，会让B72下载并让B71 receipt绑定README/LICENSE/generation metadata等非执行兄弟文件，且无法从catalog锁定已存在的严格BPE compiler manifest→在任何canonical evidence前将source收紧为七项 exact size/SHA执行闭集并绑定 `geneb-genome-ocean-bpe-v1.json` 的repo-relative path/size/SHA；重新签发receipt，缺失/额外选择/旧catalog-contract/错误tokenizer manifest均fail closed，兄弟GenomeOcean-500M既有evidence不因本行变化重签|V10,V44,V48,V53,V65
B117|2026-08-17|decoder typed artifact与ModelFile均通过后，通用 `--verify-cpu-adapter` 仍把所有artifact的 `encode("ACGT")` 硬编码为 synthetic character fixture `{0,1,2,3}`，因而错误拒绝使用同一已严格审计GenomeOcean BPE的真实4B artifact（实际 exact `{1,29,9,2}`）；这是测试夹具假设泄漏，不是runtime/tokenizer漂移→adapter门按 `model.id` 对 synthetic tiny 与已冻结GenomeOcean production tokenizer分别锁精确向量，其余profile至少要求encode成功/非空/ID入界并继续执行 typed runtime==public Context hidden逐字节比较；错误production向量仍拒绝，新增真实4B closure且不放宽converter/tokenizer独立parity门|V44,V45,V47,V53,V55
B118|2026-08-17|GENERATOR-1.2B 的B114 tokenizer/source/artifact均已严格通过，但首个冻结oracle门显示native `source_tokens=3`而上游为4；`kManualBosPlusTokenizerDefault` 已在metadata中编译却没有prepared-token语义，使GENEB字面 `<s>`+分词器BOS的double-BOS `[1,1,...]` 被静默降成 `[1,...]`，并导致 max `57.909317`/mean `0.331975794`/cosine `0.991765435`超门；其catalog prefix是符号标记 `tokenizer-bos-text`而非可直接BPE的字面 `<s>`，若将该policy加入literal-prefix路径反会产生UNK→保持 `prefix_is_literal()` 仅处理真字面 `kPrefixOnly`；ManualBos分支要求prefix精确为 `tokenizer-bos-text`、tokenizer default post-processor恰好产生一个prefix/零suffix且tokens非空，再将首个BOS token精确复制到其后并锁 `prefix_token_count=2`，manual-CLS重排不变；以两条prepared-token `[1,1,0,0]`/`[1,1,32,1397]`、prefix/policy/post-processor corruption、其他policy回归及新final-native A/B evidence锁定，不得换输入或放宽门|V44,V45,V47,V53,V55,V56
B119|2026-08-17|GenomeOcean-4B 与B99的500M共用同一pinned GENEB `GenomeOceanExtractor`，其CPU路径显式以 `torch.float32` 加载/执行，但4B profile和已转artifact仍锁 `decoder.activation_dtype=BF16`；这会在未运行oracle前已确定逐层激活语义与CPU reference不同→仅4B profile将activation dtype固定为F32，source embedding/projection/norm的BF16权重仍按位展开；废弃旧BF16-activation artifact并重转A/B，以独立Transformers4.38.2/Torch2.1.2 CPU-F32双输入oracle、dtype corruption、真实adapter与final-native evidence锁定，不得沿用BF16门或放宽容差|V44,V48,V53,V55,V56
B120|2026-08-17|NT-2.5B-MS 的B115七文件receipt逐字节通过，但pinned `vocab.txt` 与Agro同为4107项，config/embedding只有4105行；尾部id4105=`<eos>`/4106=`<bos>` 会被official EsmTokenizer字面发出并对模型OOB，而catalog/profile没有tokenizer asset/boundary，强用另一NT manifest或Agro descriptor会丢失模型级provenance→仅NT-2.5B新增专属repo compiler manifest/descriptor，复用B102的 `audited_vocab_boundary`：先验证完整4107项与full-file SHA，只发布id0..4104，精确审计尾两literal并以input-policy拒绝；相同source bytes不得静默复用Agro来源证据，以普通DNA official/native parity、两OOB literal拒绝、profile/descriptor corruption和双根artifact/evidence锁定|V10,V44,V47,V48,V53,V55,V65
B121|2026-08-17|NT-2.5B-MS 两个pinned PyTorch shard的index/keys/storage完整审计得到525 entries（524 F32+1 I64），logical bytes `10,191,212,920` 与index total_size精确一致；其中331个contiguous，其余194个全部是rank-2、offset0、full-storage、stride=`(1,rows)`的精确列主序矩阵，而profile未声明布局使strict converter必然拒绝→仅 `geneb-nt-2-5b-ms` 启用B86/B95已冻结的 `contiguous-or-exact-column-major`，锁定真实525/194 manifest与两shard分布；默认、其他stride/offset/overlap仍fail closed，sibling selected-profile contract不变，以production gate、corruption、双根artifact/evidence复验|V10,V48,V53,V55
B122|2026-08-17|LucaOne strict conversion在权重/shape之前错拒绝20层 `rot_emb.inv_freq`：checkpoint中每张都是F32[32]且payload bit-equal SHA `fbd34497ae14419f5376f818eb15303dcc48b5496124bf8acddb564310260879`，其数值精确为pinned `modeling_lucaone.py`的理想RoPE公式先round-to-FP16再upcast F32；converter却用未量化double公式比较，26/32超 `1e-7`、max `1.5867481e-4`，runtime则本来保留并直接消费源tensor→仅LucaOne的source validator改为锁定FP16-round→F32生成顺序、20层ordinal/name/shape/dtype及bit-equal payload；不改artifact/runtime RoPE语义，以理想公式误接受、单字节/缺层/额外层corruption、真实A/B conversion与oracle evidence锁定|V10,V48,V53,V55
B123|2026-08-17|NT-2.5B-MS 的官方 Transformers4.32 sharded `from_pretrained` 默认先分配约10.19GB模型再同时反序列化约9.91GB首shard，16GB主机上会在数值执行前形成不可接受峰值；其官方 `low_cpu_mem_usage=True` 路径又要求基础oracle环境未声明的 Accelerate，且meta assign会保留源194个列主序参数而标准loader会复制成row-major，临时自写loader或直接沿用该stride都会改变可复现执行路径→仅该独立oracle环境显式锁定同期 `accelerate==0.21.0` 及完整transitive package lock，继续使用官方built-in `EsmForMaskedLM.from_pretrained(...,low_cpu_mem_usage=True)`；加载后逐参数值不变 `.contiguous()`，严格要求before恰为194且全部rank2/offset0/full-storage/stride `(1,rows)`、after为0，以复刻标准官方parameter layout且峰值只增单个最大tensor；不得改权重值、算子、输入或阈值，不得将依赖扩散到共享环境，并以无绝对路径环境摘要、双次确定性输出和final-native证据锁定|V44,V53,V55
B124|2026-08-17|GENERATOR-3B pinned revision实际含13项文件，而catalog的 `required_files=null`/tokenizer assets未知会让B72抓入不执行的README、`.gitattributes`与generation config，也未冻结三片共 `11993082400` bytes Safetensors/index及与1.2B字节相同但模型级来源独立的indexed-vocab tokenizer→source allowlist仅保留config、三权重片、index、`modeling_generator.py`、special-token map、`tokenizer.py`、tokenizer config与vocab十项 exact size/SHA；为3B生成独立repo compiler manifest/descriptor，复用B114严格indexed-vocab→custom-kmer语义但不得复用1.2B raw receipt provenance；旧全仓receipt、缺片/额外选取、错误分片index或跨模型descriptor均fail closed，以双根tokenizer/artifact/evidence与final-native锁定|V10,V44,V47,V48,V53,V65
B125|2026-08-17|Evo-1-131k 的family profile已冻结15项执行来源（含三片共 `12912913568` bytes Safetensors、index、remote Hyena代码与tokenizer文件）及repo内 byte-BPE compiler manifest，但catalog仍把source/tokenizer写成未知，令B72枚举仓库兄弟文件且B71 receipt无法绑定已存在的strict descriptor→catalog仅绑定profile已锁的15项 exact size/SHA与 `geneb-evo1-byte-v1.json` path/size/SHA，清除tokenizer unknown；converter仍只从verified `conversion_assets` 子集取权重，oracle才读取其余已hash remote code，二者不得选择未receipt文件；以旧contract拒绝、asset跨根一致、1M复杂度门、真实A/B artifact/evidence及normalized逐记录语义锁定|V10,V44,V47,V48,V53,V65
B126|2026-08-17|Custom-encoder production adapter gate把任意短输入先补到 `maximum_sequence_length=4096` 做typed+public双前向，又对4096-token截断结果重复双前向；对LucaOne 1.57B/20层这等价于四次完整4096-token标量推理，未增加边界语义覆盖却使真实artifact门无界运行→Luca production仍必须在原短输入及一个有可见padding的短有界序列上逐位比较typed/public hidden与pool；4096 padding/truncation只验证prepare/token-plan的长度、mask、prefix/suffix与保留区间，不执行大模型，tiny/小fixture继续完整向量门；以超长分支零forward计数、真实Luca adapter完成时间界及既有Genomics-FM回归锁定|V45,V47,V53,V55,V56
B127|2026-08-17|METAGENE-1 pinned revision含README、图片、generation config及未被 `PreTrainedTokenizerFast` 执行的 SentencePiece fallback，catalog却仍以 `required_files=null`/tokenizer assets未知暴露；B72全仓会扩大已达 `25938674488` bytes的六片F32 source，且无法绑定实际执行的1024-vocab BPE→source allowlist仅保留config、六权重片、index、special-token map、tokenizer.json与tokenizer config十一项 exact size/SHA，明确排除`.gitattributes`/README/overview/generation config/tokenizer.model；新增模型专属strict HF-BPE compiler manifest，锁 Replace-at-start normalizer、Whitespace pretokenizer、EOS-only template、六个raw special aliases及right batch padding；旧contract、fallback-tokenizer替换、分片/index不一致和descriptor跨模型复用均拒绝，以预冻结短输入、串行双根artifact/evidence及资源峰值审计锁定|V10,V44,V47,V48,V53,V65
B128|2026-08-17|GenomeOcean-4B 的pinned CPU extractor显式要求F32，默认官方loader会先分配 `17012699136` bytes F32参数并同时反序列化最大 `4989311712` bytes shard，保守下界 `22002010848` bytes 已超过16GiB物理内存；以BF16加载虽省内存却会重现B119错误语义→独立GO4 oracle环境锁定 Transformers4.38.2/Torch2.1.2 与同期 `accelerate==0.27.2`，使用官方 `from_pretrained(...,torch_dtype=float32,low_cpu_mem_usage=True)` 逐片装载，严格断言全部参数F32、完整loading_info及两输入确定性；不得降精度、自写权重loader或扩散依赖到共享环境，若最终17GB模型加workspace仍无法在swap下完成则迁移到更大内存host而非改门，以完整package lock、portable report和final-native evidence锁定|V44,V53,V55
B129|2026-08-17|DNA-GPT-3B-M 的manual checkpoint虽预计仅 `6227969411` bytes BF16，但pinned CPU oracle会构造约 `12245049344` bytes F32模型并同时持有source state，保守下界 `18684240531` bytes 已超过16GiB物理内存；在尚无canonical SHA前用合成权重估算成功不能冒充真实可执行证据→先冻结Google Drive file ID/name/size与两输入，不取得完整SHA/receipt绝不运行；取得后必须优先执行pinned官方F32 load/forward并记录峰值，若本机swap仍失败则迁移到≥32GiB host，不得改BF16、删层、自写近似loader或放宽阈值，以完整环境锁、双次oracle与final-native A/B evidence锁定|V44,V48,V53,V55,V65
B130|2026-08-17|Evo-1-131k 的B125 source/profile虽将全仓收紧为15项，但pinned `model.py` 与 `modeling_hyena.py` 分别直接 import `.cache` 中的 `InferenceParams`/`RecurrentInferenceParams`，遗漏同revision `cache.py`（size `1378`、SHA `4aeaaf482d1da04ee9c31d47fe85bdce8e9c9e03a5cd3e183843e902c9114b73`）使独立上游oracle的verified source root不可导入且15项receipt不闭合→Evo1 catalog `required_files` 与family profile source_files同步15→16加入该exact执行源，conversion_assets仍保持原6项；旧15项catalog contract/receipt、缺失/额外/字节漂移的cache均fail closed，oracle只能从verified16项root导入，以profile↔catalog闭集相等、missing/cache corruption、canonical receipt重签及真实oracle/A/B evidence锁定|V10,V44,V48,V53,V65
B131|2026-08-17|Evo-1-131k pinned config 与 bundled GENEB inference YAML 均强制 `use_flash_attn=true`；pinned `model.py` 对 `flash_attn.modules.mha.MHA` 的ImportError仅吞掉而不提供fallback，AttentionBlock随后无条件实例化MHA，`positional_embeddings.py`也直接依赖flash-attn，因此clean Apple-arm64 CPU reference在模型构造前即不可执行，安装该包仍只能进入CUDA kernel→该行 `upstream_status=broken/reference_status=blocked`，独立CPU证据只可标 `pinned-upstream-normalized-gpu-free`：对pinned source应用可审计局部patch，仅把3个attention层的CUDA MHA替换为纯PyTorch F32 dense causal attention，严格保留官方QKV/out参数名与payload、RoPE、head划分、`1/sqrt(head_dim)` scale、causal mask、残差/归一化及其余29层Hyena数学，不import/抄native runtime；冻结clean failure摘要、原文件/patch/patched-file SHA与仅3层replacement集合，未知source/patch、非CPU-F32或额外拓扑改动拒绝，以独立operator小向量、双次上游输出、真实A/B artifact/evidence及GPU-free dependency scan锁定，绝不把normalized oracle冒充clean GENEB reference|V44,V45,V48,V53,V55,V56
B132|2026-08-17|GENERATOR-3B 的官方F32参数为 `2998262784` 个/`11993051136` bytes；历史 eager loader再叠最大 `4996117216`-byte shard的下界已达 `16989168352` bytes，16GiB不可执行，而pinned Transformers4.57.3普通 `from_pretrained` 已无条件使用builtin meta+Safetensors逐tensor assign并按三shard串行加载，省略/传入 `low_cpu_mem_usage` 等价且无需Accelerate；但继承 `HF_ENABLE_PARALLEL_LOADING=true` 会并发三片而破坏峰值界，且现generator只锁12个direct package而未拒绝transitive漂移→仅3B oracle锁CPython3.11.15与1.2B已实证的完整26-package环境，显式清空CUDA并令 `HF_ENABLE_PARALLEL_LOADING=false`，禁止device_map/Accelerate与并行loader，记录Transformers builtin-meta/serial provenance和`ru_maxrss`；正式读源后严格要求index 273项、全部CPU-F32、logical bytes/topology精确一致，短输入单线程串行双forward；不得把被忽略的low-memory flag当保证、不得降精度或放宽门，若实测峰值仍失败则迁移≥24GiB，以环境变量/package/header corruption和双次oracle/A/B evidence锁定|V44,V48,V53,V55,V65
B133|2026-08-17|DNA-GPT-3B-M 的B129“双次oracle”可被误读为两个独立模型进程，但任何native输出前冻结的preflight只定义每个输入在同一pinned官方F32模型实例上连续forward两次并要求 `torch.equal`，execution order也仅含一个official-oracle进程；首个真实进程已完成两输入各双forward且峰值footprint `12631307992` bytes，另起第二个12.6GB峰值进程既不增加已冻结算子确定性门又改变事后资源合同→B129的双次oracle精确定义为同一正式进程、同一model/input逐次两次完整forward、pooled F32逐位相等，独立第二进程不作新增条件；A/B跨进程/跨根确定性仍由fresh conversion与final-native evidence逐字节门承担；新增portable execution audit绑定catalog/receipt/checkpoint/generator/report/vector/NPY/package-lock与repeat count/result，并记录wheel及峰值资源遥测，禁止绝对路径、运行时telemetry进入canonical evidence digest或事后重写输入/阈值|V44,V48,V53,V55,V65
B134|2026-08-17|Evo-1-131k与METAGENE-1 preflight把独立CPU-F32 oracle宿主收紧为Apple-arm64且至少48/64GiB，但本机16GiB对约25.8/25.9GB的F32模型不安全，而已配置高内存宿主仅有Linux-x86_64；V45数值门要求冻结oracle图、环境、输入与阈值，并不要求oracle宿主与最终Apple native同ISA逐位一致→仅这两模型允许在经预检的Linux-x86_64高内存宿主上以exact locked env串行执行；CPU/F32、单线程、deterministic algorithms、source/patch/token/hidden/pooling、两输入与预冻结阈值全部不变，每输入同一model连续两次forward仍须 `torch.equal`，package/build/operator向量按ISA单独hash并写portable environment audit，最终Apple native仍只按冻结max-abs/mean-abs/cosine门比较；禁止跨ISA bit-exact宣称、CUDA/MPS/BF16、阈值放宽及16GiB本机试跑，preflight larger-host状态只可改为verified-high-memory-host|V44,V45,V48,V53,V55,V65
B135|2026-08-17|METAGENE独立oracle声明CPU-F32却调用Transformers4.57.3 `from_pretrained(...,device_map="auto")`；Accelerate1.12在Apple MPS可用时会把MPS纳入auto placement且容量不足可disk offload，而generator随后强制291项参数全部CPU并要求 `hf_device_map` 仅含cpu，使规划的Apple宿主合同自相矛盾且执行位置未冻结→该模型唯一改为官方loader显式 `device_map={"":"cpu"}`，保持builtin meta+Safetensors逐tensor串行assign，严格断言无CUDA/MPS/disk/meta、291项F32且logical bytes=`25938640896`；placement-only修正不得改变权重、算子、输入、双forward或阈值，以auto-MPS/auto-disk拒绝、显式CPU load及final-native evidence锁定|V44,V45,V48,V53,V55,V65
